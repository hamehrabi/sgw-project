import { HomeScreen } from '@/views/HomeScreen'

/**
 * TASK-001 delivered sign-in and an empty shell; TASK-002 fills it with the joined asset
 * view and the panel that loads a storm into it. The ranking, the planning view and the
 * dispatch board arrive with TASK-003 onward and are deliberately absent.
 */
export default function Home() {
  return <HomeScreen />
}
