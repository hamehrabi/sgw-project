[pytest]
# Tests live in the specification workspace (executable-tests.md), the application lives
# in backend/. This file is what joins the two.
testpaths = spec/03-tests/05-executable
pythonpath = backend ci
python_files = test_*.py
filterwarnings =
    error
