# Specification Change Log

> Source: Ch. 30 §30.3 (Versioning Requirements and Specs) + Ch. 24 §24.7.
> **The rule:** code must not quietly move ahead of the specification. When behavior
> changes, the requirement, test, task, and review evidence change with it.

---

## Current versions

| Artifact | Version | What changes it | Who approves | Evidence needed |
|---|---|---|---|---|
| PRD | PRD v1.1 | New requirement, changed priority, clarified non-goal. | Product owner | Change note and affected requirement IDs. |
| Technical spec | TECH v3.8 | Architecture, API, data model, or integration decision. | Technical lead or reviewer | ADR or design note. |
| Test spec | TEST v1.0 | New behavior, bug fix, edge case, failure path. | Developer and reviewer | New or updated test cases. |
| Agent rules | AGENT v1.0 | Repeated AI mistake or new coding boundary. | Team lead | Reason and example. |
| Release plan | REL v1.0 | Deployment target, rollback strategy, monitoring rule. | Release owner | Checklist update. |

---

## Change entries

```
Change ID:
Date:
Changed artifact:
Old version:
New version:
Reason for change:
Affected requirements:
Affected tests:
Affected tasks or code areas:
Decision owner:
Reviewer:
Status: proposed / accepted / rejected / deferred
Notes:
```

| Change ID | Date | Artifact | Old → New | Reason | Affected REQ | Affected TEST | Owner | Status |
|---|---|---|---|---|---|---|---|---|
| CHG-001 | 2026-08-15 | `database-design.md` + `data-and-integration-spec.md` | TECH v1.0 → v1.1 | Q-014 answered mid-run: an admin uploads prepared scenarios through the application, and several storms may be loaded at once. Both files were written while that was undecided. | REQ-F-010 | — (Round 7) | Developer | accepted |
| CHG-002 | 2026-08-15 | `fitness-functions.md` + `database-design.md` + `technical-spec.md` | TECH v1.1 → v1.2 | ADR-002 chose an embedded store, which has no role system. BR-004's grant-based enforcement became unrunnable, so ADR-004 replaced it with database triggers and FF-004's check was rewritten. The two separate database credentials in §7.5 no longer exist. | BR-004, REQ-F-009, REQ-R-002 | — (Round 7) | Developer | accepted |
| CHG-003 | 2026-08-15 | `fitness-functions.md` | TECH v1.2 → v1.3 | Round 5 named "all seven known data defects are caught" as a release gate. Nothing in the register covered it, so FF-006 was added. | REQ-NF-003 | — (Round 7) | Developer | accepted |
| CHG-004 | 2026-08-15 | `technical-spec.md` §7.1, §7.2 | TECH v1.3 → v1.4 | Round 6 confirmed no external services, removing email delivery. The reset-link flow ADR-003 implied became unsatisfiable, so password recovery is an admin-set temporary password and the RBAC matrix gained a row. Derived rather than chosen — see Q-024. | REQ-NF-002, SEC-A-004, SEC-Z-006 | — (Round 7) | Developer | accepted |
| CHG-005 | 2026-08-15 | `05-architecture/` — new ADR-005, index and decision log updated | TECH v1.4 → v1.5 | Q-023 answered during Round 7, after the task plan showed TASK-003 blocked with nothing to unblock it. Version one scores with a deterministic weighted rule behind the existing model boundary; deep learning rejected for this scorer on explainability; decision-tree family named as successor. Raises Q-025. | REQ-F-002, REQ-F-003, BR-002 | EVAL-001 | Developer | accepted |
| CHG-007 | 2026-08-15 | `05-architecture/` — new ADR-008 and ADR-009; CON-006 amended; `.env.example`, `runtime-and-scale.md`, `fitness-functions.md`, `ai-boundary-spec.md`, `technical-spec.md` | TECH v2.0 → v3.0 | **Two decisions taken after the intake closed.** ADR-008 moves the backend to Python/FastAPI with Next.js as a separate frontend — amending ADR-001's one-process claim and ADR-002's driver, and resolving the background-job and trigger-generation gaps. ADR-009 introduces a hosted OpenAI model that **phrases computed reasons and may invent nothing**, reversing CON-006 and Round 6's "no external services", and reopening Q-019. Adds FF-007. Raises Q-029, Q-030. | REQ-F-003, BR-002, REQ-NF-003, REQ-NF-007 | FF-007 | Developer | accepted |
| CHG-008 | 2026-08-15 | `database-design.md` §1, §3, §5, §6, §7 | TECH v3.0 → v3.1 | **A gap found by TASK-001, the first task to build against the schema.** ADR-003 requires a session created, checked, and ended server-side; ADR-006 twice describes "a session lookup per request against a local store"; ADR-002 holds that nothing durable lives in process memory. §3 defined no table for one. A `sessions` table is added, storing **a hash of the session value and never the value itself**, which is what keeps Q-007's prohibition true of a durable table. Expiry cannot be a constraint — both ADR-006 limits are relative to the current time — so §3 now names the API layer's single session check as the enforcement point and STEST-002 as the test that fails if it stops working. **No new decision was taken:** the table is what three accepted ADRs already required and no document had written down. | REQ-NF-002, SEC-A-001, SEC-A-002, CON-003 | STEST-001, STEST-002 | Developer | accepted |
| CHG-009 | 2026-08-15 | `api-specification.md` — endpoint index and auth-model line | TECH v3.1 → v3.2 | **Two endpoints the build needed and the index did not carry, found by TASK-001.** `GET /api/v1/auth/session` returns the current user and role: `frontend-component-spec.md` requires `AppShell` to render no content until the role is known, and after a page reload the browser holds a session cookie and nothing else, so the role was unrecoverable. The alternative — keeping the role where the browser can read it — puts a permission input in the one place `technical-spec.md` §3 forbids. `GET /api/v1/health` was required by TASK-001's expected output and already assumed by `runtime-and-scale.md` §1. Both are additive, which the versioning table calls a safe change. The auth-model line still read "session lifetime is not yet set — see Q-021"; ADR-006 answered it and the line is corrected. **Raised by the agent during implementation and accepted at the TASK-001 review**, on the ground that the endpoint follows directly from storing sessions server-side: once CHG-008 made the session a durable row, reading it back is the same decision rather than a new one. | REQ-NF-002, REQ-R-001, REQ-NF-003 | STEST-002 | Developer | accepted |
| CHG-010 | 2026-08-15 | `fitness-functions.md` — FF-002 restated; wiring note added | TECH v3.2 → v3.3 | **A dead gate, found at the TASK-001 review.** FF-002 read *no view module imports the scoring module directly*, which described the single-process codebase ADR-001 assumed. ADR-008 put the views in a different language in a different process, so the check became one that **cannot fail** — and a fitness function that cannot fail governs nothing, which is the precise decay this register exists to catch. Restated as two checks that can: **(a)** `scoring` is imported by `api` and nothing else, inside the backend; **(b)** no ADR-007 constant — the four weights, the band boundaries, the reason-strength thresholds — appears anywhere in `frontend/`. Half (b) is the failure ADR-008's own consequences predicted: "the scoring module is never reimplemented, mirrored, or partially duplicated in the frontend for display purposes". Retiring the row outright was the alternative and was declined: REQ-NF-005 still needs a guard, and the risk moved rather than disappeared. Also records that TASK-002 wires FF-001 and FF-006. | REQ-NF-005 | — (FF-002 is the assertion) | Developer | accepted |
| CHG-011 | 2026-08-15 | `data-and-integration-spec.md` §1 — three columns added to the prepared-scenario format | TECH v3.3 → v3.4 | **Two of the seven defect rules could not be represented in the format, found while preparing TASK-002.** §4 requires the loader to refuse a percentage built on a broken customer total (defect 4) and to flag an impossible outage count (defect 5), and FF-006 gates on catching 7 of 7 against a fixture carrying all seven. `outages.csv` carried `asset_id, failure_time, storm_id` — no total to be broken, no population to exceed. Adds `customers_out` and `service_area_id` to `outages.csv`, and a `service_areas[]` block to `manifest.json` carrying the independent population figure defect 4 explicitly requires. **This knowingly breaks Q-017's "every column traces to the source PRD §7 dataset table; nothing invented"** — the alternative was retiring two rules the source PRD measured in real public files, and leaving FF-006 permanently unreachable while it read as Planned. The population figure sits in the manifest rather than in a fifth CSV so that Q-017's *manifest plus four CSVs* still describes the format. | REQ-F-001, REQ-F-010 | UTEST-006, UTEST-007, FF-006 | Developer | accepted |
| CHG-012 | 2026-08-15 | `database-design.md` §1, §3, §7 — `scenario_uploads` added | TECH v3.4 → v3.5 | **The parse job had nowhere to keep its own state, found while building TASK-002.** `technical-spec.md` §9.5 specifies the job completely — a stored upload identifier, the states *uploading → parsing → ready* or *failed*, and a failure naming the file — while §9.1 requires that a parse failing partway creates **no scenario at all**. A job whose state hangs off the scenario row therefore has no home during the only period anyone is watching it, and none at all if it fails. Adds `scenario_uploads`, with two check constraints putting the rules in the schema rather than in the service layer (ADR-002): a `ready` upload must name a scenario, a `failed` one must name a file. **No new decision:** the table is what §9.5 already described and no document had written down — the third instance of that pattern after CHG-008 and CHG-009. | REQ-F-010, REQ-NF-003 | FTEST-001, STEST-005, STEST-006 | Developer | accepted |
| CHG-013 | 2026-08-15 | `requirements.md` (REQ-NF-003, AC-002), `frontend-component-spec.md` (`StalenessBanner`), `fitness-functions.md` (FF-003), `failure-tests.md` (FTEST-002), `.env.example` | TECH v3.5 → v3.6 | **"Last good picture" meant two different things, and one of them could not happen.** Raised at design time, before the views were written. `technical-spec.md` §6 already requires every read to be served from stored results, so a source file lost *after* load cannot reach a screen — which made three clauses unreachable: REQ-NF-003's "name which file failed", FTEST-002's naming assertion, and `StalenessBanner`'s trigger (*the last good picture shown instead of current data* — there is no "instead"). **FF-003 was worse than unreachable: it was vacuously true**, the same defect CHG-010 found in FF-002, caught one task earlier this time. Splits the requirement into **(a) age** — every screen states how old its data is, escalating to a non-dismissible banner past `SCENARIO_STALE_AFTER_HOURS` — and **(b) integrity** — a lost file is named to an admin and reported as affecting replay and recovery, while the joined view stays correct rather than degrading. The files are not worthless after parse: `outages.csv` is `ai-evals.md`'s replay input and `technical-spec.md` §12 defines a backup as the database **plus** the uploaded files. **`SCENARIO_STALE_AFTER_HOURS = 6`**, because the National Hurricane Center issues full advisories every 6 hours — a property of the forecast source rather than an inference about operators. **P1: 3 hours during an active storm** (NHC intermediate advisories). **Revisit if the forecast source changes.** **Also adds `scenarios.forecast_issued_at`** (`database-design.md` §1, §3; migration 004, nullable per §8): the age every screen states is measured from the manifest's issue time, the loader had always parsed it, and nothing stored it — so the origin of the claim did not survive the load. That is the fourth instance of the pattern in `AGENT.md`'s third lessons row, **found one step after that row was written, by the person who wrote it**, which is the strongest argument available that the check belongs in Prepare rather than in good intentions. | REQ-NF-003, REQ-F-001, AC-002, AC-010 | FTEST-002, FTEST-003, ATEST-010, FF-003 | Developer | accepted |
| CHG-014 | 2026-08-15 | `backend/app/scoring/references.py` (new); corrects a factual claim in ADR-007 | TECH v3.6 → v3.7 | **Two of ADR-007's four factors compare against reference values that exist nowhere.** *Forecast gust vs asset **design threshold*** and *age vs **expected service life***: neither is a CSV column, a config value, or a constant in any document. ADR-007 states "the four factors map exactly onto the four CSVs in a prepared scenario, so nothing needs an input the fixture does not carry" — **that claim is wrong for these two**, and it is corrected here rather than in the ADR, which is immutable once accepted. Resolved as **per-asset-type engineering constants**, held in the same config block as the weights and carrying their sources: ASCE 7 basic wind speeds by risk category, and typical utility asset service lives. Per-asset columns were the alternative and were declined — 220 invented numbers with no source is worse than 4 with one, and it would make the fixture author the engineering authority. **These constants are uncalibrated on exactly the same footing as the weights**, are stated as such on screen, and share their exit condition: calibration with SGW's engineers. This is the fifth instance of `AGENT.md`'s third lessons row — a computation specified without its inputs. | REQ-F-002, REQ-F-003 | UTEST-009, UTEST-010, FTEST-004, EVAL-001 | Developer | accepted |
| CHG-015 | 2026-08-15 | `requirements.md` AC-009; `security-tests.md` STEST-005 | TECH v3.7 → v3.8 | **AC-009 asked for something the audit table cannot hold.** It required a refused upload to be recorded, and STEST-005 read that as a `decision_records` row — but `decision_records.scenario_id` is `not null`, and a *refused* upload has no scenario, because refusing it is what stopped one being created. Found at the TASK-004 review. **Decided by the developer:** the refusal is recorded in the **security log** — actor, time, filename, reason — and AC-009's wording is corrected to say so. The reasoning, which is the part worth keeping: *the decision record holds decisions about recommendations, and a refused upload is an access-control event, not a decision.* Making `scenario_id` nullable was the cheap fix and was **declined** — the not-null constraint is part of what makes the audit table trustworthy, and loosening it for one event type trades a structural guarantee for a convenience. A separate one-event table was also declined as not worth its own schema. **Intent preserved, audit table intact.** | REQ-R-001, SEC-Z-002 | STEST-005 | Developer | accepted |
| CHG-016 | 2026-08-16 | `api-specification.md` — one row added to the endpoint index | TECH v3.8 → v3.9 (**proposed**) | **Nothing brings a damage report into existence, found while preparing TASK-005.** `database-design.md` §3 defines `damage_reports` in full; the endpoint index carries `GET /scenarios/{id}/jobs` (read the board) and `POST /damage-reports/{id}/dismiss` (TASK-008) and no way for a report to be created. The prepared files cannot be the source either: `data-and-integration-spec.md` §1 says `outages.csv` is *historical, replay only* and *feeds nothing at run time*, its rows are 2024 failures rather than calls taken during this storm, and it carries no reporter. So **AC-007's *given* — "two damage reports arrive for the same location" — has no way to occur**, ATEST-007 and ITEST-003 are unreachable, and `DispatchBoard`'s empty state is its only state. Adds **`POST /api/v1/scenarios/{scenario_id}/damage-reports`**, signed in and **not privileged** (the dispatcher holds `user`, and REQ-R-001 gives them the board). Body: a `neighbourhood` and an optional `asset_id` — every other field is refused rather than dropped. Additive, which the versioning table calls a safe change, and **the same shape as CHG-009**: an endpoint the build needed that the index did not carry. **Declined:** *deriving reports from `outages.csv` at load* — it would make the dispatch board a replay of a 2024 storm rather than a picture of this one, contradicting §1, and every report would be actorless; *seeding reports through the store in tests only* — ITEST-003's integration point is **API + database**, so a store-only path leaves half the row untested and the feature unusable by a person; *no creation path at all* — REQ-F-007 would ship as a permanently empty screen, which is the one thing `frontend-component-spec.md` says that screen must never look like. Implemented by TASK-005 (`api/dispatch.py`); **acceptance owed.** | REQ-F-007, AC-007, REQ-R-001 | ATEST-007, ITEST-003 | Developer | **proposed** |
| CHG-017 | 2026-08-16 | `database-design.md` §3 — `repair_jobs.location_key` added; the resolution of `damage_reports.location` fixed | TECH v3.8 → v3.9 (**proposed**) | **Two things §3 leaves undecided that AC-007 and REQ-NF-007 both need decided, found while preparing TASK-005.** **(a) Half of AC-007 had nowhere to be enforced.** §2 rightly calls `damage_reports.repair_job_id` *the single nullable link that makes AC-007 structural* — a report cannot belong to two jobs. But nothing in §3 stops two **jobs** existing for one location, which is the half that matters for *no crew is sent to the same place twice*, and ADR-002 forbids leaving it in service code: "never implement a check in the service layer that the store could refuse". Adds **`repair_jobs.location_key`** — the normalised neighbourhood a job answers — with **`unique (scenario_id, location_key)`**. The find-first lookup in `store/dispatch.py` becomes an optimisation of that constraint rather than the rule itself. Case and spacing are normalised into the key, because a capital letter is not a second location and the cost of treating it as one is a second crew. **(b) The report's location had no resolution.** §3 says `location: json, required` and stops there, while CON-003 forbids storing *any premise-level record* and REQ-NF-007 requires damage locations *aggregated to neighbourhood level in every log and export*. Fixes the column to exactly `{"neighbourhood": "<text>"}`, enforced by a check constraint that rebuilds the object and compares it — an address, a meter id, a coordinate or a household field is refused **by the database**. Two consequences, both intended: **"the same location" in AC-007 means the same neighbourhood**, which is the finest location this platform may legally hold; and **REQ-NF-007 stops being a rule every log line has to remember**, because nothing finer is stored to leak. **Declined:** *a display `location` column on `repair_jobs`* — two places to disagree about one fact, so the board derives a job's neighbourhood from its first report; *storing a finer location and aggregating on the way out* — the aggregation becomes a step every log and export path must remember, and the requirement fails the first time one forgets, which is precisely what ADR-002 says not to build; *keying the report to a `service_areas` row* — the manifest's `service_areas[]` is parsed and never stored, so this adds a table and a load path TASK-002 did not build, and a real report may name an area no manifest listed; *enforcing the shape in the API layer only* — the first direct insert, or the first refactor, removes it. Implemented by migration 007; **acceptance owed.** | REQ-F-007, REQ-NF-007, AC-007, CON-003 | ATEST-007, ITEST-003, UTEST-012, STEST-009 | Developer | **proposed** |
| CHG-018 | 2026-08-16 | `database-design.md` §3 — a `seq` column on `repair_jobs`, `damage_reports` and `decision_records` | TECH v3.9 → v3.10 (**proposed**) | **The order three tables are read in was not an order, found when the second review of TASK-005 ran the suite twice.** Every chronological read in the store was `order by <timestamp>, id`. `datetime.now(UTC).isoformat()` resolves to about **15.6 ms** on this platform — 1,999 of 2,000 consecutive calls return an identical string — and the tiebreak `id` is a random UUID hex, so two rows written inside one tick came back in **coin-flip order**. Five of fifteen clean `pytest` runs on an unmodified tree were red, from two tests: ATEST-007's *"in the order they were called in — the queue is the history"* and ITEST-002's *"ordered by when they happened"*. **This is not TASK-005's defect alone.** `decision_records` has had it since migration 006 shipped: `read_all`'s docstring claims *"the order **is** the history, not a view of it"* — it was not — and `latest_recommendation`'s `order by occurred_at desc limit 1` could return the **wrong row outright**, which would record a decision against a recommendation nobody was shown. Adds **`seq integer`**, a monotonic per-table sequence allocated by the single writer inside the insert statement (ADR-002), **`unique`** so two rows claiming one place in the history are refused by the database rather than resolved by chance. Reads become `order by seq`. **Declined:** *a higher-resolution clock* — resolution is a property of the platform, and the next platform brings the defect back; *a lexicographically sortable identifier (ULID)* — it moves the tiebreak into a generator holding state in process memory, which ADR-002 says nothing durable may depend on; *SQLite's implicit `rowid`* — it is not in the schema, `VACUUM` may renumber it, and an audit trail's order should not rest on an implementation detail; *leaving `decision_records` alone and fixing only the board* — the older half is the one that carries regulatory evidence. **One consequence is deliberate and is recorded rather than worked around:** `decision_records` **cannot be backfilled**, because BR-004's `BEFORE UPDATE` trigger refuses the statement. Rows written before this migration keep `seq = 0` and the tie they already had; a partial unique index (`where seq > 0`) enforces the rest. Implemented by migration 008; **acceptance owed.** | REQ-F-007, REQ-F-009, BR-004, AC-007 | ATEST-007, ITEST-002, ITEST-003 | Developer | **proposed** |
| CHG-019 | 2026-08-16 | `database-design.md` §3 — `damage_reports.asset_id` and `.repair_job_id` become composite foreign keys; two unique parent-key indexes added | TECH v3.9 → v3.10 (**proposed**) | **A foreign key that proves existence and never membership — the second review's Block.** §3 gives `damage_reports.asset_id` a foreign key to `assets.id` and `repair_job_id` one to `repair_jobs.id`. Both prove the row exists somewhere; **neither proves it belongs to the storm the report names.** Issued directly against the schema, a storm-A report could name storm-B's asset *and* hang off storm-B's repair job; only a non-existent `scenario_id` was refused. The only thing stopping it was `if body.asset_id and not scenarios.find_asset(...)` in `api/dispatch.py` — a rule in the service layer that the store could refuse, which is ADR-002's exact prohibition and the condition `review-log.md` pre-committed to blocking on. Disabling that branch left 248 tests passing and nothing red. CLAUDE.md calls a missing scope *"a correctness bug — two storms blended into one ranking would look entirely plausible"*, and TASK-005 is where it first becomes reachable from **caller-supplied input**. Adds **`unique (id, scenario_id)`** on `assets` and on `repair_jobs` — SQLite will only accept a composite foreign key against a unique parent key — and rewrites the two child keys as **`(asset_id, scenario_id) → assets (id, scenario_id)`** and **`(repair_job_id, scenario_id) → repair_jobs (id, scenario_id)`**. A null half still satisfies the constraint, so §4's *a report naming no matching asset is still a report* is untouched. The service-layer lookup stays as the legible 400 in front of the constraint, the way `store/dispatch.py`'s find-first lookup is an optimisation of `unique (scenario_id, location_key)` rather than the rule. **Declined:** *keeping the check in the API only* — that is the finding, not a fix; *`on delete set null`* — SQLite can only null a composite child key whole and `scenario_id` is `not null`, so `on delete cascade` is used, which is what §7 already specifies (reports, jobs and assets are hard-deleted with their scenario, the only path that deletes any of them); *`deferrable initially deferred`* — the refusal would surface at commit rather than at the statement, which makes "the database refuses this insert" harder to assert and harder to read; *a trigger* — a check the schema can express should not be a trigger. **The parent-key indexes are ordered `(id, scenario_id)` rather than `(scenario_id, id)` deliberately**: the latter serves `where scenario_id = ?` as well as the board's own index, the planner would sometimes choose it, and PTEST-002's `repair_jobs_scenario_status` assertion would go back to being unfailable. **Not fixed here and recorded rather than left silent:** `risk_scores.asset_id` (migration 005) has the same shape. It is written only from server-derived identifiers, never from caller input, and rewriting it is outside a TASK-005 remediation. Implemented by migration 008; **acceptance owed.** | REQ-F-007, AC-007, REQ-F-010 | ITEST-003, ITEST-005 | Developer | **proposed** |
| CHG-020 | 2026-08-16 | `database-design.md` §3 / `api-specification.md` — the board's job location is derived from the first report *filed*; `dismissed_report_count` added to the board response | TECH v3.9 → v3.10 (**proposed**) | **A described state the board could reach and could not render.** Dismiss a repair job's only report — TASK-008's state, whose `dismissed_by` / `dismissed_reason` columns migration 007 already carries and which the schema permits **today** — and `GET /scenarios/{id}/jobs` returned that job as `location: {"neighbourhood": null}` with `report_count: 0`: work on a shared dispatcher's board with no location and nothing behind it. **CHG-017 declined a display column on `repair_jobs`** on the ground that *"the board derives a job's neighbourhood from its first report"*, and the decision stands — but the derivation read the *working* list, which is not what that sentence says. Dismissal hides a report from the working list; it does not unsay where the job is, and the dismissed report still holds the neighbourhood. Fixed by deriving the location from **the first report ever filed against the job, whatever its status**, and by carrying **`dismissed_report_count`** so a job with nothing open reads as *explained* rather than as empty. The board query returns every status and the split happens in `api/views.py`, so it stays two statements whatever the report count (PTEST-002). **Declined:** *reversing CHG-017 and adding a display column* — two stored places to disagree about one fact, and the fact is already stored; *deriving the name from `repair_jobs.location_key`* — the key is casefolded to `saltmarsh` for grouping, so a board built from it would shout a neighbourhood's name back at the dispatcher in the wrong case; *hiding a job whose reports are all dismissed* — whether such a job leaves the board is TASK-008's decision, and quietly removing work from a shared board is the more dangerous of the two guesses; *leaving `null`* — the mutation showed the reach exactly: forcing every location to `null` turns ITEST-003's two-locations test red, so the happy path was asserted and this one was not. Implemented by `api/views.py`; **acceptance owed, and it bears on CHG-017, which is still proposed.** | REQ-F-007, REQ-F-008, AC-007 | ITEST-003, UTEST-011 | Developer | **proposed** |
| CHG-021 | 2026-08-16 | `database-design.md` §3 — the meaning of `damage_reports.status = 'duplicate'` fixed | TECH v3.9 → v3.10 (**proposed**) | **An enumerated value with no writer and no reader.** §3 permits `status in ('open','duplicate','dismissed')`. Nothing writes `duplicate`, and the board's filter was `status <> 'dismissed'`, so a duplicate-marked report rendered as ordinary open work and was counted as a second piece of open work in the neighbourhood figure REQ-NF-007 governs. Not wrong today, because nothing sets it — but a value the schema permits is a state the board can reach, and a state the board can reach is one it has to render. Fixes the meaning: **a `duplicate` is a repeat call about damage already on the board.** It **stays visible** (losing a second radio call is the failure AC-007's second half exists to prevent) and carries its status so the screen badges it; and it is **not counted** by `open_reports_in_area`, because counting a repeat call twice overstates the area's open work. **Declined:** *removing the value from the enumeration* — §3 defines it and this is not the task that gets to narrow the schema; *giving it a writer* — no requirement asks for one, TASK-008 writes `dismissed` and not this, and inventing an endpoint would be inventing a feature; *leaving it undefined* — a value with no reader is the shape this review keeps finding, and the cost of naming it now is one filter and one test. No migration: the constraint is unchanged and only the reader is. Implemented by `store/dispatch.py`; **acceptance owed.** | REQ-NF-007, REQ-F-008 | UTEST-012, ITEST-003 | Developer | **proposed** |
| CHG-022 | 2026-08-16 | `api-specification.md` — the board response carries reports that belong to no repair job; `frontend-component-spec.md` (`DispatchBoard`) renders them | TECH v3.9 → v3.10 (**proposed**) | **A damage report with no repair job exists in the schema, appears on no screen, and is missing from the figure REQ-NF-007 governs. Found at the third review of TASK-005, by the check for a described state with nowhere to live.** `database-design.md` §3 makes `damage_reports.repair_job_id` **optional** and §1 says a report *"Belongs to one scenario, optionally to one asset, and **to at most one repair job**."* Nothing decides what the board does when it holds none. Today it does nothing: `api/views.py`'s `board_body` groups reports by `repair_job_id` and then emits **one item per job**, so reports whose link is null fall into a bucket keyed `None` that is never read. Issued directly against the database — the only way to reach the state, because `store/dispatch.py` writes report and job in one transaction — a storm with two open reports returns `report_count: 1`, the second report is on no screen, and `open_reports_in_area`, an **inner** join through `repair_jobs`, logs `open_reports_in_area=1` for a neighbourhood that has two. All 264 tests stay green with that row present. **A report nobody can find is the radio call AC-007's second half exists to keep**, and *an empty screen must never read as safety* (CLAUDE.md). Fixes it the way CHG-021 fixed `status = 'duplicate'` eight rows above, and for the same stated reason — *a value the schema permits is a state the board can reach, and a state the board can reach is one it has to render*: the board response gains a group for reports that belong to no job, `report_count` counts them, and `open_reports_in_area` counts an open report in its own neighbourhood whether or not a job has been raised for it. **Declined:** *making `repair_job_id` `not null` so the store refuses the row* — it is the tighter constraint and ADR-002 would ordinarily prefer it, but §2 calls the column *"the single **nullable** link that makes AC-007 structural"* and §3 calls it optional, so narrowing it is a decision about what a damage report **is**, not an implementation detail, and CHG-021 declined the parallel act of narrowing an enumeration §3 defines; it remains the better answer if a human decides that "optional" was incidental rather than intended. *Leaving it* — the state is unreachable through the endpoint **today**, which is exactly what was said about the storm-scope hole one review ago, and TASK-008's dismissal is the next task to write to this table. *Hiding such reports deliberately and saying so* — a shared dispatch board that silently drops work is the one thing `frontend-component-spec.md` says that screen must never do. No migration: the schema is unchanged and only the reader is. **Not implemented — this entry is a finding, and the fix belongs to whoever answers the Block.** | REQ-F-007, REQ-NF-007, AC-007 | ATEST-007, ITEST-003, UTEST-012 | Developer | **proposed** |
| CHG-006 | 2026-08-15 | 20 files across `01-docs/`, `02-tasks/`, `03-tests/`, `07-ops/`; new ADR-006 and ADR-007 | TECH v1.5 → v2.0 | **Fourteen open questions answered after the intake closed.** Q-017 (scenario = manifest + four CSVs, under 5 MB) unblocked TASK-002 and eight downstream tasks. Q-027 (Next.js/TypeScript + SQLite) filled every command in `07-ops/`. Q-007 enumerated the forbidden data. Q-021 and Q-025 became ADR-006 and ADR-007. Q-012 gave measurable limits; Q-013 gave REQ-NF-006 a standard and ATEST-011; Q-015 set retention; Q-011, Q-019, Q-022, Q-024 closed. Q-018 and Q-028 deliberately left open with reasons. | REQ-NF-001, REQ-NF-004, REQ-NF-006, REQ-NF-007, SEC-A-002, SEC-A-004, SEC-A-006, CON-003 | ATEST-011, PTEST-001, PTEST-002 | Developer | accepted |

---

## Stage acceptance and skips

Two things are recorded here as **dated rows**, and neither is ever a file of its own: the
acceptance of each round's gate, and any blueprint deliberately skipped with its reason.

A separate acceptance file would be a second place to look for the same fact, and the two
would disagree within a week. A row in the log that already exists cannot.

**The date is the first column.** That is what makes a row findable — an acceptance buried
in a nine-column change entry is not a record anyone can check, and the change-entries table
above starts with an identifier rather than a date, so it cannot serve.

| Date | Stage or type | Artifact | Note or reason |
|---|---|---|---|
| 2026-08-15 | Round 1 — the idea | — | Accepted by the developer. 7 decisions, 2 inferences, 6 TODOs (Q-001 to Q-006). |
| 2026-08-15 | Round 2 — scope boundaries | — | Accepted by the developer. 6 decisions, 1 inference, 4 new TODOs (Q-007 to Q-010); Q-001 to Q-004 closed. |
| 2026-08-15 | Round 3 — users, roles, and data | — | Accepted by the developer. 8 decisions, 4 inferences, 6 new TODOs (Q-011 to Q-016); Q-010 closed. |
| 2026-08-15 | Round 4 — product shape | — | Accepted by the developer. 9 decisions, 2 inferences, 3 new TODOs (Q-018 to Q-020); Q-005 closed. |
| 2026-08-15 | Round 5 — architecture and stack | — | Accepted by the developer. 7 decisions (ADR-001 to ADR-004, DD-001 to DD-007), 1 inference, 2 new TODOs (Q-021, Q-022); Q-016 and Q-020 closed. CHG-002 and CHG-003 raised. |
| 2026-08-15 | Round 6 — security, reliability, integrations | — | Accepted by the developer. 8 decisions (SEC-A-001 to SEC-A-006, SEC-Z-001 to SEC-Z-006), 3 inferences, 2 new TODOs (Q-023, Q-024). CHG-004 raised. `ai-boundary-spec.md` filled rather than skipped. |
| 2026-08-15 | Round 7 — tasks and tests | — | Accepted by the developer. 11 decisions (TASK-001 to TASK-010, A-001 to A-015, 47 test ids), 2 inferences, 1 new TODO (Q-025); Q-009 and Q-023 closed. CHG-005 raised, adding ADR-005 out of round. `ai-evals.md` filled rather than skipped. |
| 2026-08-15 | Round 8 — operations | — | Accepted by the developer. 9 decisions, 1 inference, 1 new TODO (Q-026), deferred at the developer's direction: no real people exist, one person holds every role for the prototype, rollback triggers self-approved, naming left as a production-planning TODO rather than invented. Final round; 39 files written. |

**A skip with no reason is a silent skip wearing a label.** The reason is what lets a later
reader tell a decision from an omission.

---

## When implementation reveals something the spec missed (Ch. 15 §15.8)

| When this happens | Update this document |
|---|---|
| A rule becomes clearer during implementation | Requirements document |
| A design decision changes | Technical specification or ADR |
| A new test case is discovered | Test plan |
| A task produces extra work | Task plan and traceability matrix |
| A behavior is removed or postponed | Scope and out-of-scope notes |

## When production teaches you something (Ch. 3 §3.9)

| Change type | Artifact to update |
|---|---|
| A new user behavior is added. | Requirements and product specification. |
| A data field or relationship changes. | Technical specification and data model. |
| A new security rule is added. | Requirements, technical specification, test plan. |
| A bug reveals missing expected behavior. | Requirement, test plan, task history. |
| Deployment process changes. | Deployment checklist and maintenance notes. |

---

## Spec update fields (Ch. 24 §24.7)

| Field | What to write |
|---|---|
| Change date | When the spec was updated. |
| Reason | Bug fix, user feedback, performance issue, security finding, product decision. |
| Affected requirement | The requirement ID or section that changed. |
| Affected tests | Which tests need to be added or changed. |
| Affected code area | The module, endpoint, page, job, or service connected to the change. |
| Review status | Draft, reviewed, approved, implemented, or released. |

---

> **Spec drift warning (Ch. 15 §15.8):** spec drift happens when the code changes but the
> specification stays behind. The longer you allow drift, the harder it becomes to trust
> the source of truth for your project.

---

> Blueprint: blueprints/01-docs/09-change-control/spec-change-log.md
