# Specification Change Log

> Source: Ch. 30 §30.3 (Versioning Requirements and Specs) + Ch. 24 §24.7.
> **The rule:** code must not quietly move ahead of the specification. When behavior
> changes, the requirement, test, task, and review evidence change with it.

---

## Current versions

| Artifact | Version | What changes it | Who approves | Evidence needed |
|---|---|---|---|---|
| PRD | PRD v1.1 | New requirement, changed priority, clarified non-goal. | Product owner | Change note and affected requirement IDs. |
| Technical spec | TECH v3.8 | Architecture, API, data model, or integration decision. | Technical lead or reviewer | ADR or design note. |
| Test spec | TEST v1.0 | New behavior, bug fix, edge case, failure path. | Developer and reviewer | New or updated test cases. |
| Agent rules | AGENT v1.0 | Repeated AI mistake or new coding boundary. | Team lead | Reason and example. |
| Release plan | REL v1.0 | Deployment target, rollback strategy, monitoring rule. | Release owner | Checklist update. |

---

## Change entries

```
Change ID:
Date:
Changed artifact:
Old version:
New version:
Reason for change:
Affected requirements:
Affected tests:
Affected tasks or code areas:
Decision owner:
Reviewer:
Status: proposed / accepted / rejected / deferred
Notes:
```

| Change ID | Date | Artifact | Old → New | Reason | Affected REQ | Affected TEST | Owner | Status |
|---|---|---|---|---|---|---|---|---|
| CHG-001 | 2026-08-15 | `database-design.md` + `data-and-integration-spec.md` | TECH v1.0 → v1.1 | Q-014 answered mid-run: an admin uploads prepared scenarios through the application, and several storms may be loaded at once. Both files were written while that was undecided. | REQ-F-010 | — (Round 7) | Developer | accepted |
| CHG-002 | 2026-08-15 | `fitness-functions.md` + `database-design.md` + `technical-spec.md` | TECH v1.1 → v1.2 | ADR-002 chose an embedded store, which has no role system. BR-004's grant-based enforcement became unrunnable, so ADR-004 replaced it with database triggers and FF-004's check was rewritten. The two separate database credentials in §7.5 no longer exist. | BR-004, REQ-F-009, REQ-R-002 | — (Round 7) | Developer | accepted |
| CHG-003 | 2026-08-15 | `fitness-functions.md` | TECH v1.2 → v1.3 | Round 5 named "all seven known data defects are caught" as a release gate. Nothing in the register covered it, so FF-006 was added. | REQ-NF-003 | — (Round 7) | Developer | accepted |
| CHG-004 | 2026-08-15 | `technical-spec.md` §7.1, §7.2 | TECH v1.3 → v1.4 | Round 6 confirmed no external services, removing email delivery. The reset-link flow ADR-003 implied became unsatisfiable, so password recovery is an admin-set temporary password and the RBAC matrix gained a row. Derived rather than chosen — see Q-024. | REQ-NF-002, SEC-A-004, SEC-Z-006 | — (Round 7) | Developer | accepted |
| CHG-005 | 2026-08-15 | `05-architecture/` — new ADR-005, index and decision log updated | TECH v1.4 → v1.5 | Q-023 answered during Round 7, after the task plan showed TASK-003 blocked with nothing to unblock it. Version one scores with a deterministic weighted rule behind the existing model boundary; deep learning rejected for this scorer on explainability; decision-tree family named as successor. Raises Q-025. | REQ-F-002, REQ-F-003, BR-002 | EVAL-001 | Developer | accepted |
| CHG-007 | 2026-08-15 | `05-architecture/` — new ADR-008 and ADR-009; CON-006 amended; `.env.example`, `runtime-and-scale.md`, `fitness-functions.md`, `ai-boundary-spec.md`, `technical-spec.md` | TECH v2.0 → v3.0 | **Two decisions taken after the intake closed.** ADR-008 moves the backend to Python/FastAPI with Next.js as a separate frontend — amending ADR-001's one-process claim and ADR-002's driver, and resolving the background-job and trigger-generation gaps. ADR-009 introduces a hosted OpenAI model that **phrases computed reasons and may invent nothing**, reversing CON-006 and Round 6's "no external services", and reopening Q-019. Adds FF-007. Raises Q-029, Q-030. | REQ-F-003, BR-002, REQ-NF-003, REQ-NF-007 | FF-007 | Developer | accepted |
| CHG-008 | 2026-08-15 | `database-design.md` §1, §3, §5, §6, §7 | TECH v3.0 → v3.1 | **A gap found by TASK-001, the first task to build against the schema.** ADR-003 requires a session created, checked, and ended server-side; ADR-006 twice describes "a session lookup per request against a local store"; ADR-002 holds that nothing durable lives in process memory. §3 defined no table for one. A `sessions` table is added, storing **a hash of the session value and never the value itself**, which is what keeps Q-007's prohibition true of a durable table. Expiry cannot be a constraint — both ADR-006 limits are relative to the current time — so §3 now names the API layer's single session check as the enforcement point and STEST-002 as the test that fails if it stops working. **No new decision was taken:** the table is what three accepted ADRs already required and no document had written down. | REQ-NF-002, SEC-A-001, SEC-A-002, CON-003 | STEST-001, STEST-002 | Developer | accepted |
| CHG-009 | 2026-08-15 | `api-specification.md` — endpoint index and auth-model line | TECH v3.1 → v3.2 | **Two endpoints the build needed and the index did not carry, found by TASK-001.** `GET /api/v1/auth/session` returns the current user and role: `frontend-component-spec.md` requires `AppShell` to render no content until the role is known, and after a page reload the browser holds a session cookie and nothing else, so the role was unrecoverable. The alternative — keeping the role where the browser can read it — puts a permission input in the one place `technical-spec.md` §3 forbids. `GET /api/v1/health` was required by TASK-001's expected output and already assumed by `runtime-and-scale.md` §1. Both are additive, which the versioning table calls a safe change. The auth-model line still read "session lifetime is not yet set — see Q-021"; ADR-006 answered it and the line is corrected. **Raised by the agent during implementation and accepted at the TASK-001 review**, on the ground that the endpoint follows directly from storing sessions server-side: once CHG-008 made the session a durable row, reading it back is the same decision rather than a new one. | REQ-NF-002, REQ-R-001, REQ-NF-003 | STEST-002 | Developer | accepted |
| CHG-010 | 2026-08-15 | `fitness-functions.md` — FF-002 restated; wiring note added | TECH v3.2 → v3.3 | **A dead gate, found at the TASK-001 review.** FF-002 read *no view module imports the scoring module directly*, which described the single-process codebase ADR-001 assumed. ADR-008 put the views in a different language in a different process, so the check became one that **cannot fail** — and a fitness function that cannot fail governs nothing, which is the precise decay this register exists to catch. Restated as two checks that can: **(a)** `scoring` is imported by `api` and nothing else, inside the backend; **(b)** no ADR-007 constant — the four weights, the band boundaries, the reason-strength thresholds — appears anywhere in `frontend/`. Half (b) is the failure ADR-008's own consequences predicted: "the scoring module is never reimplemented, mirrored, or partially duplicated in the frontend for display purposes". Retiring the row outright was the alternative and was declined: REQ-NF-005 still needs a guard, and the risk moved rather than disappeared. Also records that TASK-002 wires FF-001 and FF-006. | REQ-NF-005 | — (FF-002 is the assertion) | Developer | accepted |
| CHG-011 | 2026-08-15 | `data-and-integration-spec.md` §1 — three columns added to the prepared-scenario format | TECH v3.3 → v3.4 | **Two of the seven defect rules could not be represented in the format, found while preparing TASK-002.** §4 requires the loader to refuse a percentage built on a broken customer total (defect 4) and to flag an impossible outage count (defect 5), and FF-006 gates on catching 7 of 7 against a fixture carrying all seven. `outages.csv` carried `asset_id, failure_time, storm_id` — no total to be broken, no population to exceed. Adds `customers_out` and `service_area_id` to `outages.csv`, and a `service_areas[]` block to `manifest.json` carrying the independent population figure defect 4 explicitly requires. **This knowingly breaks Q-017's "every column traces to the source PRD §7 dataset table; nothing invented"** — the alternative was retiring two rules the source PRD measured in real public files, and leaving FF-006 permanently unreachable while it read as Planned. The population figure sits in the manifest rather than in a fifth CSV so that Q-017's *manifest plus four CSVs* still describes the format. | REQ-F-001, REQ-F-010 | UTEST-006, UTEST-007, FF-006 | Developer | accepted |
| CHG-012 | 2026-08-15 | `database-design.md` §1, §3, §7 — `scenario_uploads` added | TECH v3.4 → v3.5 | **The parse job had nowhere to keep its own state, found while building TASK-002.** `technical-spec.md` §9.5 specifies the job completely — a stored upload identifier, the states *uploading → parsing → ready* or *failed*, and a failure naming the file — while §9.1 requires that a parse failing partway creates **no scenario at all**. A job whose state hangs off the scenario row therefore has no home during the only period anyone is watching it, and none at all if it fails. Adds `scenario_uploads`, with two check constraints putting the rules in the schema rather than in the service layer (ADR-002): a `ready` upload must name a scenario, a `failed` one must name a file. **No new decision:** the table is what §9.5 already described and no document had written down — the third instance of that pattern after CHG-008 and CHG-009. | REQ-F-010, REQ-NF-003 | FTEST-001, STEST-005, STEST-006 | Developer | accepted |
| CHG-013 | 2026-08-15 | `requirements.md` (REQ-NF-003, AC-002), `frontend-component-spec.md` (`StalenessBanner`), `fitness-functions.md` (FF-003), `failure-tests.md` (FTEST-002), `.env.example` | TECH v3.5 → v3.6 | **"Last good picture" meant two different things, and one of them could not happen.** Raised at design time, before the views were written. `technical-spec.md` §6 already requires every read to be served from stored results, so a source file lost *after* load cannot reach a screen — which made three clauses unreachable: REQ-NF-003's "name which file failed", FTEST-002's naming assertion, and `StalenessBanner`'s trigger (*the last good picture shown instead of current data* — there is no "instead"). **FF-003 was worse than unreachable: it was vacuously true**, the same defect CHG-010 found in FF-002, caught one task earlier this time. Splits the requirement into **(a) age** — every screen states how old its data is, escalating to a non-dismissible banner past `SCENARIO_STALE_AFTER_HOURS` — and **(b) integrity** — a lost file is named to an admin and reported as affecting replay and recovery, while the joined view stays correct rather than degrading. The files are not worthless after parse: `outages.csv` is `ai-evals.md`'s replay input and `technical-spec.md` §12 defines a backup as the database **plus** the uploaded files. **`SCENARIO_STALE_AFTER_HOURS = 6`**, because the National Hurricane Center issues full advisories every 6 hours — a property of the forecast source rather than an inference about operators. **P1: 3 hours during an active storm** (NHC intermediate advisories). **Revisit if the forecast source changes.** **Also adds `scenarios.forecast_issued_at`** (`database-design.md` §1, §3; migration 004, nullable per §8): the age every screen states is measured from the manifest's issue time, the loader had always parsed it, and nothing stored it — so the origin of the claim did not survive the load. That is the fourth instance of the pattern in `AGENT.md`'s third lessons row, **found one step after that row was written, by the person who wrote it**, which is the strongest argument available that the check belongs in Prepare rather than in good intentions. | REQ-NF-003, REQ-F-001, AC-002, AC-010 | FTEST-002, FTEST-003, ATEST-010, FF-003 | Developer | accepted |
| CHG-014 | 2026-08-15 | `backend/app/scoring/references.py` (new); corrects a factual claim in ADR-007 | TECH v3.6 → v3.7 | **Two of ADR-007's four factors compare against reference values that exist nowhere.** *Forecast gust vs asset **design threshold*** and *age vs **expected service life***: neither is a CSV column, a config value, or a constant in any document. ADR-007 states "the four factors map exactly onto the four CSVs in a prepared scenario, so nothing needs an input the fixture does not carry" — **that claim is wrong for these two**, and it is corrected here rather than in the ADR, which is immutable once accepted. Resolved as **per-asset-type engineering constants**, held in the same config block as the weights and carrying their sources: ASCE 7 basic wind speeds by risk category, and typical utility asset service lives. Per-asset columns were the alternative and were declined — 220 invented numbers with no source is worse than 4 with one, and it would make the fixture author the engineering authority. **These constants are uncalibrated on exactly the same footing as the weights**, are stated as such on screen, and share their exit condition: calibration with SGW's engineers. This is the fifth instance of `AGENT.md`'s third lessons row — a computation specified without its inputs. | REQ-F-002, REQ-F-003 | UTEST-009, UTEST-010, FTEST-004, EVAL-001 | Developer | accepted |
| CHG-015 | 2026-08-15 | `requirements.md` AC-009; `security-tests.md` STEST-005 | TECH v3.7 → v3.8 | **AC-009 asked for something the audit table cannot hold.** It required a refused upload to be recorded, and STEST-005 read that as a `decision_records` row — but `decision_records.scenario_id` is `not null`, and a *refused* upload has no scenario, because refusing it is what stopped one being created. Found at the TASK-004 review. **Decided by the developer:** the refusal is recorded in the **security log** — actor, time, filename, reason — and AC-009's wording is corrected to say so. The reasoning, which is the part worth keeping: *the decision record holds decisions about recommendations, and a refused upload is an access-control event, not a decision.* Making `scenario_id` nullable was the cheap fix and was **declined** — the not-null constraint is part of what makes the audit table trustworthy, and loosening it for one event type trades a structural guarantee for a convenience. A separate one-event table was also declined as not worth its own schema. **Intent preserved, audit table intact.** | REQ-R-001, SEC-Z-002 | STEST-005 | Developer | accepted |
| CHG-016 | 2026-08-16 | `api-specification.md` — one row added to the endpoint index | TECH v3.8 → v3.9 (**proposed**) | **Nothing brings a damage report into existence, found while preparing TASK-005.** `database-design.md` §3 defines `damage_reports` in full; the endpoint index carries `GET /scenarios/{id}/jobs` (read the board) and `POST /damage-reports/{id}/dismiss` (TASK-008) and no way for a report to be created. The prepared files cannot be the source either: `data-and-integration-spec.md` §1 says `outages.csv` is *historical, replay only* and *feeds nothing at run time*, its rows are 2024 failures rather than calls taken during this storm, and it carries no reporter. So **AC-007's *given* — "two damage reports arrive for the same location" — has no way to occur**, ATEST-007 and ITEST-003 are unreachable, and `DispatchBoard`'s empty state is its only state. Adds **`POST /api/v1/scenarios/{scenario_id}/damage-reports`**, signed in and **not privileged** (the dispatcher holds `user`, and REQ-R-001 gives them the board). Body: a `neighbourhood` and an optional `asset_id` — every other field is refused rather than dropped. Additive, which the versioning table calls a safe change, and **the same shape as CHG-009**: an endpoint the build needed that the index did not carry. **Declined:** *deriving reports from `outages.csv` at load* — it would make the dispatch board a replay of a 2024 storm rather than a picture of this one, contradicting §1, and every report would be actorless; *seeding reports through the store in tests only* — ITEST-003's integration point is **API + database**, so a store-only path leaves half the row untested and the feature unusable by a person; *no creation path at all* — REQ-F-007 would ship as a permanently empty screen, which is the one thing `frontend-component-spec.md` says that screen must never look like. Implemented by TASK-005 (`api/dispatch.py`); **acceptance owed.** | REQ-F-007, AC-007, REQ-R-001 | ATEST-007, ITEST-003 | Developer | **proposed** |
| CHG-017 | 2026-08-16 | `database-design.md` §3 — `repair_jobs.location_key` added; the resolution of `damage_reports.location` fixed | TECH v3.8 → v3.9 (**proposed**) | **Two things §3 leaves undecided that AC-007 and REQ-NF-007 both need decided, found while preparing TASK-005.** **(a) Half of AC-007 had nowhere to be enforced.** §2 rightly calls `damage_reports.repair_job_id` *the single nullable link that makes AC-007 structural* — a report cannot belong to two jobs. But nothing in §3 stops two **jobs** existing for one location, which is the half that matters for *no crew is sent to the same place twice*, and ADR-002 forbids leaving it in service code: "never implement a check in the service layer that the store could refuse". Adds **`repair_jobs.location_key`** — the normalised neighbourhood a job answers — with **`unique (scenario_id, location_key)`**. The find-first lookup in `store/dispatch.py` becomes an optimisation of that constraint rather than the rule itself. Case and spacing are normalised into the key, because a capital letter is not a second location and the cost of treating it as one is a second crew. **(b) The report's location had no resolution.** §3 says `location: json, required` and stops there, while CON-003 forbids storing *any premise-level record* and REQ-NF-007 requires damage locations *aggregated to neighbourhood level in every log and export*. Fixes the column to exactly `{"neighbourhood": "<text>"}`, enforced by a check constraint that rebuilds the object and compares it — an address, a meter id, a coordinate or a household field is refused **by the database**. Two consequences, both intended: **"the same location" in AC-007 means the same neighbourhood**, which is the finest location this platform may legally hold; and **REQ-NF-007 stops being a rule every log line has to remember**, because nothing finer is stored to leak. **Declined:** *a display `location` column on `repair_jobs`* — two places to disagree about one fact, so the board derives a job's neighbourhood from its first report; *storing a finer location and aggregating on the way out* — the aggregation becomes a step every log and export path must remember, and the requirement fails the first time one forgets, which is precisely what ADR-002 says not to build; *keying the report to a `service_areas` row* — the manifest's `service_areas[]` is parsed and never stored, so this adds a table and a load path TASK-002 did not build, and a real report may name an area no manifest listed; *enforcing the shape in the API layer only* — the first direct insert, or the first refactor, removes it. Implemented by migration 007; **acceptance owed.** | REQ-F-007, REQ-NF-007, AC-007, CON-003 | ATEST-007, ITEST-003, UTEST-012, STEST-009 | Developer | **proposed** |
| CHG-018 | 2026-08-16 | `database-design.md` §3 — a `seq` column on `repair_jobs`, `damage_reports` and `decision_records` | TECH v3.9 → v3.10 (**proposed**) | **The order three tables are read in was not an order, found when the second review of TASK-005 ran the suite twice.** Every chronological read in the store was `order by <timestamp>, id`. `datetime.now(UTC).isoformat()` resolves to about **15.6 ms** on this platform — 1,999 of 2,000 consecutive calls return an identical string — and the tiebreak `id` is a random UUID hex, so two rows written inside one tick came back in **coin-flip order**. Five of fifteen clean `pytest` runs on an unmodified tree were red, from two tests: ATEST-007's *"in the order they were called in — the queue is the history"* and ITEST-002's *"ordered by when they happened"*. **This is not TASK-005's defect alone.** `decision_records` has had it since migration 006 shipped: `read_all`'s docstring claims *"the order **is** the history, not a view of it"* — it was not — and `latest_recommendation`'s `order by occurred_at desc limit 1` could return the **wrong row outright**, which would record a decision against a recommendation nobody was shown. Adds **`seq integer`**, a monotonic per-table sequence allocated by the single writer inside the insert statement (ADR-002), **`unique`** so two rows claiming one place in the history are refused by the database rather than resolved by chance. Reads become `order by seq`. **Declined:** *a higher-resolution clock* — resolution is a property of the platform, and the next platform brings the defect back; *a lexicographically sortable identifier (ULID)* — it moves the tiebreak into a generator holding state in process memory, which ADR-002 says nothing durable may depend on; *SQLite's implicit `rowid`* — it is not in the schema, `VACUUM` may renumber it, and an audit trail's order should not rest on an implementation detail; *leaving `decision_records` alone and fixing only the board* — the older half is the one that carries regulatory evidence. **One consequence is deliberate and is recorded rather than worked around:** `decision_records` **cannot be backfilled**, because BR-004's `BEFORE UPDATE` trigger refuses the statement. Rows written before this migration keep `seq = 0` and the tie they already had; a partial unique index (`where seq > 0`) enforces the rest. Implemented by migration 008; **acceptance owed.** | REQ-F-007, REQ-F-009, BR-004, AC-007 | ATEST-007, ITEST-002, ITEST-003 | Developer | **proposed** |
| CHG-019 | 2026-08-16 | `database-design.md` §3 — `damage_reports.asset_id` and `.repair_job_id` become composite foreign keys; two unique parent-key indexes added | TECH v3.9 → v3.10 (**proposed**) | **A foreign key that proves existence and never membership — the second review's Block.** §3 gives `damage_reports.asset_id` a foreign key to `assets.id` and `repair_job_id` one to `repair_jobs.id`. Both prove the row exists somewhere; **neither proves it belongs to the storm the report names.** Issued directly against the schema, a storm-A report could name storm-B's asset *and* hang off storm-B's repair job; only a non-existent `scenario_id` was refused. The only thing stopping it was `if body.asset_id and not scenarios.find_asset(...)` in `api/dispatch.py` — a rule in the service layer that the store could refuse, which is ADR-002's exact prohibition and the condition `review-log.md` pre-committed to blocking on. Disabling that branch left 248 tests passing and nothing red. CLAUDE.md calls a missing scope *"a correctness bug — two storms blended into one ranking would look entirely plausible"*, and TASK-005 is where it first becomes reachable from **caller-supplied input**. Adds **`unique (id, scenario_id)`** on `assets` and on `repair_jobs` — SQLite will only accept a composite foreign key against a unique parent key — and rewrites the two child keys as **`(asset_id, scenario_id) → assets (id, scenario_id)`** and **`(repair_job_id, scenario_id) → repair_jobs (id, scenario_id)`**. A null half still satisfies the constraint, so §4's *a report naming no matching asset is still a report* is untouched. The service-layer lookup stays as the legible 400 in front of the constraint, the way `store/dispatch.py`'s find-first lookup is an optimisation of `unique (scenario_id, location_key)` rather than the rule. **Declined:** *keeping the check in the API only* — that is the finding, not a fix; *`on delete set null`* — SQLite can only null a composite child key whole and `scenario_id` is `not null`, so `on delete cascade` is used, which is what §7 already specifies (reports, jobs and assets are hard-deleted with their scenario, the only path that deletes any of them); *`deferrable initially deferred`* — the refusal would surface at commit rather than at the statement, which makes "the database refuses this insert" harder to assert and harder to read; *a trigger* — a check the schema can express should not be a trigger. **The parent-key indexes are ordered `(id, scenario_id)` rather than `(scenario_id, id)` deliberately**: the latter serves `where scenario_id = ?` as well as the board's own index, the planner would sometimes choose it, and PTEST-002's `repair_jobs_scenario_status` assertion would go back to being unfailable. **Not fixed here and recorded rather than left silent:** `risk_scores.asset_id` (migration 005) has the same shape. It is written only from server-derived identifiers, never from caller input, and rewriting it is outside a TASK-005 remediation. Implemented by migration 008; **acceptance owed.** | REQ-F-007, AC-007, REQ-F-010 | ITEST-003, ITEST-005 | Developer | **proposed** |
| CHG-020 | 2026-08-16 | `database-design.md` §3 / `api-specification.md` — the board's job location is derived from the first report *filed*; `dismissed_report_count` added to the board response | TECH v3.9 → v3.10 (**proposed**) | **A described state the board could reach and could not render.** Dismiss a repair job's only report — TASK-008's state, whose `dismissed_by` / `dismissed_reason` columns migration 007 already carries and which the schema permits **today** — and `GET /scenarios/{id}/jobs` returned that job as `location: {"neighbourhood": null}` with `report_count: 0`: work on a shared dispatcher's board with no location and nothing behind it. **CHG-017 declined a display column on `repair_jobs`** on the ground that *"the board derives a job's neighbourhood from its first report"*, and the decision stands — but the derivation read the *working* list, which is not what that sentence says. Dismissal hides a report from the working list; it does not unsay where the job is, and the dismissed report still holds the neighbourhood. Fixed by deriving the location from **the first report ever filed against the job, whatever its status**, and by carrying **`dismissed_report_count`** so a job with nothing open reads as *explained* rather than as empty. The board query returns every status and the split happens in `api/views.py`, so it stays two statements whatever the report count (PTEST-002). **Declined:** *reversing CHG-017 and adding a display column* — two stored places to disagree about one fact, and the fact is already stored; *deriving the name from `repair_jobs.location_key`* — the key is casefolded to `saltmarsh` for grouping, so a board built from it would shout a neighbourhood's name back at the dispatcher in the wrong case; *hiding a job whose reports are all dismissed* — whether such a job leaves the board is TASK-008's decision, and quietly removing work from a shared board is the more dangerous of the two guesses; *leaving `null`* — the mutation showed the reach exactly: forcing every location to `null` turns ITEST-003's two-locations test red, so the happy path was asserted and this one was not. Implemented by `api/views.py`; **acceptance owed, and it bears on CHG-017, which is still proposed.** | REQ-F-007, REQ-F-008, AC-007 | ITEST-003, UTEST-011 | Developer | **proposed** |
| CHG-021 | 2026-08-16 | `database-design.md` §3 — the meaning of `damage_reports.status = 'duplicate'` fixed | TECH v3.9 → v3.10 (**proposed**) | **An enumerated value with no writer and no reader.** §3 permits `status in ('open','duplicate','dismissed')`. Nothing writes `duplicate`, and the board's filter was `status <> 'dismissed'`, so a duplicate-marked report rendered as ordinary open work and was counted as a second piece of open work in the neighbourhood figure REQ-NF-007 governs. Not wrong today, because nothing sets it — but a value the schema permits is a state the board can reach, and a state the board can reach is one it has to render. Fixes the meaning: **a `duplicate` is a repeat call about damage already on the board.** It **stays visible** (losing a second radio call is the failure AC-007's second half exists to prevent) and carries its status so the screen badges it; and it is **not counted** by `open_reports_in_area`, because counting a repeat call twice overstates the area's open work. **Declined:** *removing the value from the enumeration* — §3 defines it and this is not the task that gets to narrow the schema; *giving it a writer* — no requirement asks for one, TASK-008 writes `dismissed` and not this, and inventing an endpoint would be inventing a feature; *leaving it undefined* — a value with no reader is the shape this review keeps finding, and the cost of naming it now is one filter and one test. No migration: the constraint is unchanged and only the reader is. Implemented by `store/dispatch.py`; **acceptance owed.** | REQ-NF-007, REQ-F-008 | UTEST-012, ITEST-003 | Developer | **proposed** |
| CHG-022 | 2026-08-16 | `api-specification.md` — the board response carries reports that belong to no repair job; `frontend-component-spec.md` (`DispatchBoard`) renders them | TECH v3.9 → v3.10 (**proposed**) | **A damage report with no repair job exists in the schema, appears on no screen, and is missing from the figure REQ-NF-007 governs. Found at the third review of TASK-005, by the check for a described state with nowhere to live.** `database-design.md` §3 makes `damage_reports.repair_job_id` **optional** and §1 says a report *"Belongs to one scenario, optionally to one asset, and **to at most one repair job**."* Nothing decides what the board does when it holds none. Today it does nothing: `api/views.py`'s `board_body` groups reports by `repair_job_id` and then emits **one item per job**, so reports whose link is null fall into a bucket keyed `None` that is never read. Issued directly against the database — the only way to reach the state, because `store/dispatch.py` writes report and job in one transaction — a storm with two open reports returns `report_count: 1`, the second report is on no screen, and `open_reports_in_area`, an **inner** join through `repair_jobs`, logs `open_reports_in_area=1` for a neighbourhood that has two. All 264 tests stay green with that row present. **A report nobody can find is the radio call AC-007's second half exists to keep**, and *an empty screen must never read as safety* (CLAUDE.md). Fixes it the way CHG-021 fixed `status = 'duplicate'` eight rows above, and for the same stated reason — *a value the schema permits is a state the board can reach, and a state the board can reach is one it has to render*: the board response gains a group for reports that belong to no job, `report_count` counts them, and `open_reports_in_area` counts an open report in its own neighbourhood whether or not a job has been raised for it. **Declined:** *making `repair_job_id` `not null` so the store refuses the row* — it is the tighter constraint and ADR-002 would ordinarily prefer it, but §2 calls the column *"the single **nullable** link that makes AC-007 structural"* and §3 calls it optional, so narrowing it is a decision about what a damage report **is**, not an implementation detail, and CHG-021 declined the parallel act of narrowing an enumeration §3 defines; it remains the better answer if a human decides that "optional" was incidental rather than intended. *Leaving it* — the state is unreachable through the endpoint **today**, which is exactly what was said about the storm-scope hole one review ago, and TASK-008's dismissal is the next task to write to this table. *Hiding such reports deliberately and saying so* — a shared dispatch board that silently drops work is the one thing `frontend-component-spec.md` says that screen must never do. No migration: the schema is unchanged and only the reader is. **Implemented at the third remediation** (`api/views.py`'s `board_body`, `store/dispatch.py`'s `open_reports_in_area`, `lib/api.ts`, `DispatchBoard.tsx`): the board response carries `unattached_reports`, `report_count` and `dismissed_report_count` include them, the area figure counts an open report in its own neighbourhood whether or not a job holds the key, and the screen's empty state is only reached when there is nothing on the board at all rather than no *job* on it. **Acceptance still owed** — the entry was raised as a finding and the fix does not accept it. | REQ-F-007, REQ-NF-007, AC-007 | ATEST-007, ITEST-003, UTEST-012 | Developer | **proposed** |
| CHG-023 | 2026-08-16 | `database-design.md` §3 — `repair_jobs.location_key` must be stored normalised; `damage_reports.location`'s neighbourhood must be too; one length bound governs both | TECH v3.10 → v3.11 (**proposed**) | **The rule that decides which two locations are one was not in the store — the third review's Block, and the same Block condition as the review before it.** CHG-017 added `unique (scenario_id, location_key)` and called it AC-007. It is only part of it: that constraint refuses a **byte-identical** key, while the casefold-and-collapse that *defines* "the same location" lived entirely in `store/dispatch.py:location_key()`. Issued directly against the database beside a stored `northgate`, the store **accepted** `Northgate`, and it accepted `north  gate`, and the board then rendered `job_count: 2` for one neighbourhood — two crews at one place during a storm, which is the single failure AC-007 exists to prevent. Done criterion 3 says the second job is *"refused by the store — not by the service layer"*, and it was not. The reach of the suite was exactly one test: deleting `.casefold()` turned `ITEST-003::test_the_same_place_written_differently_is_still_one_place` red, and that test files both reports **through the endpoint**, so no store-level assertion existed at all. Fixed with a **check constraint that the stored key is already normalised** — lower case, no leading or trailing whitespace, no run of two spaces, and no tab, newline, vertical tab, form feed or carriage return anywhere in it — so the only key `repair_jobs` will hold is the one `location_key()` produces and `unique (scenario_id, location_key)` has no second spelling left to miss. **Two further things fall out of the same change and are part of it.** *(a)* The same normalisation is applied to `damage_reports.location`'s neighbourhood: writing the missing test for CON-003's fourth clause showed that `length(trim(…)) between 1 and 120` refused `"   "` and **accepted `"\t\n"`**, because SQLite's `trim()` strips spaces only — the same non-place wearing a different whitespace character. It also makes CHG-022's new area figure exact rather than approximate: an unattached report's neighbourhood is read from this column, so it has to be the same fact as the key. *(b)* **One bound, tied to its copies.** 120 was three hard-coded numbers — the two schema checks and `dispatch.NEIGHBOURHOOD_MAX` — with nothing connecting them; leaving the schema at 120 and setting the constant to 5000 turns the specified `400 validation_error` into a `500 internal_error` for a 121-character neighbourhood and the whole suite stayed green. UTEST-012 now reads the bound out of `sqlite_master` and requires all three to agree, and the endpoint measures the **key** as well as the display name, because `'ß'.casefold()` is `'ss'` and casefolding can push a legal name over the bound. **Declined:** *`unique (scenario_id, location_key collate nocase)`*, which is what the review named — it is unreachable beside the check (`nocase` folds ASCII `A–Z` and nothing else, and every string the check admits is already ASCII-lower-cased), so adding it would be a fifth deliberate instance of the shape this project keeps finding after FF-002, FF-003 and TASK-002's two defect rules: a clause that cannot fail. It is also insufficient alone — with only the collation, a direct `Northgate` into an empty table is *accepted*, and the next report filed for that neighbourhood looks the key up by `binary`, misses, tries to create a second job and gets a `500`. *A trigger* — a rule the schema can express should not be a trigger (CHG-019's reasoning, reused). *Normalising on the way out* — the aggregation-on-read failure CHG-017 already declined. *Leaving it in the service* — that is the finding, not a fix. **A limit recorded rather than implied away:** SQLite's `lower()` is ASCII-only, so a **direct insert** of a non-ASCII capital is still not folded by the schema; every key the application writes goes through `str.casefold()`, which is not ASCII-only, and closing the remainder needs an ICU build. Implemented by migration 009; **acceptance owed.** | REQ-F-007, REQ-NF-007, AC-007, CON-003 | ITEST-003, UTEST-012, ATEST-007 | Developer | **proposed** |
| CHG-024 | 2026-08-16 | `database-design.md` §3 / §4 — what happens to a damage report when the asset it names is deleted | TECH v3.10 → v3.11 (**proposed**) | **A delete behaviour that changed as a side effect of a fix for something else, raised at the third review of TASK-005 as an observation rather than a finding, and recorded here rather than left in a review row.** Migration 007 gave `damage_reports.asset_id` `on delete set null`: deleting an asset kept the report, which is §4's *"a report naming no matching asset is still a report"*. Migration 008 replaced it with `on delete cascade` — justified in CHG-019 for `repair_job_id`, where a composite child key cannot be nulled by halves, and applied to `asset_id` in the same breath without its own argument. Under 008 deleting an asset **destroys** the report. **Inert today**: there is no `delete from` statement anywhere in `backend/`, and the only deletion path any document describes is `technical-spec.md` §7.2's *delete or replace a scenario*, where the report is removed by its own `scenario_id` cascade regardless. It becomes live the moment §7.2 is built. **The decision taken here is to keep `on delete cascade` and say why, rather than change it inside a remediation for a different defect.** The alternatives are all worse or unavailable: *`on delete set null`* cannot be expressed — SQLite nulls a composite child key **whole**, and `scenario_id` is `not null`, so the statement aborts rather than nulling the asset half; *`on delete no action`* (refuse the delete) would protect the report, but the two cascades a scenario delete fires — assets, and damage reports — are processed in an order SQLite does not define, so it can turn §7.2's supported operation into an integrity error that only shows up under load; *dropping the composite key back to `references assets (id)`* reinstates CHG-019's Block, which is the more dangerous of the two defects by a wide margin. **What is owed is a decision about §4's sentence**, which is about a report the **join could not match at load** and is being read as a rule about deletion — two different claims that happen to share a phrase. If a human decides deletion is in scope, the answer is a `before delete` trigger on `assets` that refuses while a report names the row, and that is a decision about what an asset delete *means*, not an implementation detail. No migration. **Not a code change — this entry is the record.** | REQ-F-007, REQ-F-010 | ITEST-003 | Developer | **proposed** |
| CHG-025 | 2026-08-16 | `database-design.md` §1, §3 — `scenario_forecast_revisions` and `scenario_forecast_cells` added; `data-and-integration-spec.md` §1 — what `weather.csv`'s `valid_time` column means; `api-specification.md` — the response of `POST /scenarios/{id}/forecast-revisions` and two fields on `GET /scenarios/{id}` | TECH v3.11 → v3.12 (**proposed**) | **A scenario's forecast series had nowhere to live, and nothing decided what "the scenario's next forecast change" is. Found while preparing TASK-006 — `AGENT.md`'s third lessons row for the sixth time, and the reason it is checked at *Prepare*.** REQ-F-004 asks for a re-rank against *"a changed forecast **inside** the prepared scenario"*, `api-specification.md` carries `POST /scenarios/{id}/forecast-revisions` — *"apply the scenario's **next** forecast change"* — and `database-design.md` §3 already says `scenarios.forecast_revision` increments *"rather than overwriting what was ranked before"*. The change is therefore in the file: `data-and-integration-spec.md` §1 gives `weather.csv` a **`valid_time`** column and `technical-spec.md` sizes the fixture at *"220 assets, **~5,000 forecast rows**"*, which is two orders of magnitude more rows than one forecast of a 40-cell grid needs. **But `loader/load.py` took one gust per grid cell — the first row it met — and discarded every other row, and `assets` holds one `wind_gust_mph`.** So the endpoint had nothing to apply, and the only reachable implementations were re-scoring the same numbers (a second revision identical to the first, which is not a re-rank) or taking a forecast from the caller (which makes a ranking a function of untrusted input — a different product, and one REQ-F-004 explicitly does not describe). **Four things are decided here and each was reachable more than one way.** **(a) A forecast revision is one distinct `valid_time`** among the cell-level rows of `weather.csv`, numbered from 0 in chronological order; revision 0 is the earliest, which is the forecast TASK-002 already loads. **(b) Each revision is a complete grid**: a cell with no row at that time keeps the value it was last issued, carried forward **once, at load**. *Declined: sparse revisions* — a grid square that goes quiet has not stopped forecasting, and with 40 cells and 43 forecast times in the demo-scale storm almost every asset would arrive UNSCORED at every revision after the first, so "the list re-ranks" would mean "the list empties", which is the empty screen CLAUDE.md forbids reading as safety. *Declined: falling back at read time* — that is precisely `technical-spec.md` §7.3's *"never a silent fallback to the current revision"*, one link out; carrying forward is a statement about what a revision **contains**, made once, and the read has no fallback at all (`assets_with_forecast` joins one revision and takes null when it misses). **(c) The carried value keeps the `valid_time` it was ISSUED at**, not the revision's, and that time now travels with the gust as its `observed_at` — BR-003 wants the age of every value, the gust had none before, and a six-hour-old forecast rendered as current is exactly the quiet wrongness REQ-NF-003 exists to prevent. **(d) Two tables, not one column.** `scenario_forecast_revisions` holds *one revision is one forecast time* as a primary key plus `unique (scenario_id, valid_time)`; `scenario_forecast_cells` hangs off it by a **composite** foreign key over `(scenario_id, forecast_revision)`, which is CHG-019's lesson applied before it is needed rather than after — a plain `references … (forecast_revision)` would prove a revision number exists *somewhere*. *Declined: a JSON series column on `scenarios`* — a repeated group in a column, which no constraint can reach and no join can use. *Declined: re-reading `weather.csv` from `scenario_uploads.storage_path` at re-rank time* — `technical-spec.md` §6 serves every read from stored rows and CHG-013 decided that a lost source file leaves the picture **correct**; re-ranking from the file would make a lost file break a *requirement* rather than only replay and recovery, and `integrity()`'s `affects: ["replay", "recovery"]` would silently become wrong. *Declined: overwriting `assets.wind_gust_mph` as each revision is applied* — that is rewriting revision n, which is the one thing AC-005 forbids; the asset row keeps the forecast it was loaded with and the joined asset view stays REQ-F-001's picture of the storm **as loaded**, dated by the same `valid_time`. **Two consequences, both intended.** Revision 0's numbers are now written in one place and joined from it, so the joined view and the revision-0 ranking cannot drift apart; and the demo-scale generator's filler forecast rows, dated **12–14 August against an issue time of the 15th**, are moved after it — the storm's own revision 0 was a forecast issued three days before the one its manifest names, which nothing noticed because nothing read the series. **A limit recorded rather than implied away:** nothing in the schema can assert that a cell's `valid_time` is at or before its revision's, because a `check` cannot see another table; the loader is the only writer and builds both from the same pass. Implemented by migration 010, `store/forecasts.py`, `loader/load.py`, `api/rerank.py`; **acceptance owed.** | REQ-F-004, REQ-F-001, AC-005, BR-003, REQ-NF-003 | ATEST-005, ITEST-004, PTEST-001 | Developer | **proposed** |
| CHG-026 | 2026-08-16 | `database-design.md` §3 — a `before update` trigger on `risk_scores` | TECH v3.11 → v3.12 (**proposed**) | **AC-005's second half was enforced by the absence of a statement. Found while building TASK-006, and it is the standing Block condition in `review-log.md` — *a rule enforced in the service layer that the store could refuse*.** *"The previous order remains retrievable for comparison"* is the half that makes a re-rank safe to press during a storm: a crew was placed against revision 0 and a decision was recorded against it, and if applying revision 1 rewrote revision 0 the audit trail would stay intact while its subject changed underneath it — `test-specification.md` names it as ATEST-005's risk in one line, *"a re-rank destroying the order a decision was made against"*. What the schema actually held was `unique (scenario_id, asset_id, forecast_revision)`, which refuses a **second row** for a revision and has no opinion whatsoever about an `UPDATE` to the first. The rule lived in the fact that no code issued one — and ADR-002 exists because "a rule that lives only in code is removed by the first refactor with every test still green". Adds **`risk_scores_no_update`**, a `before update` trigger that aborts, in the same shape ADR-004 uses for `decision_records` and asserted the same way STEST-008 asserts BR-004: by issuing the statement against the database and requiring the refusal. **Declined: a `before delete` twin.** `risk_scores` is cascade-deleted with its scenario and with its assets, which is what `technical-spec.md` §7.2's *delete or replace a scenario* needs; a delete guard would turn a supported operation into an integrity error, and deleting a whole storm is not the failure AC-005 is about. *Declined: leaving it to the service* — that is the finding. *Declined: making the rule "no update after the revision is superseded"* — a trigger cannot cheaply ask whether a later revision exists, and the narrower rule would permit rewriting the **current** ranking, which is the one a decision is most likely to be pending against. *Declined: raising it as an observation rather than a change* — CHG-024 was recorded that way because it changed no code; this one narrows what the schema will accept, which is a decision about what a stored ranking **is**. **The test written for this was wrong on its first run and the mutation is what said so, which is worth keeping in the record:** it set `score = 0.0` across a whole revision, and that includes the UNSCORED row, which BR-002's `json_array_length(reasons) >= 1 or score is null` refuses whether this trigger exists or not — same exception type, same test result, with the trigger removed. It now names a scored row and reads the rule out of the refusal's message. That is the fifth *assertion that could not fail for the reason it claimed* this repository has found, and the second found in an ordinary test. Implemented by migration 010; **acceptance owed.** | REQ-F-004, REQ-F-009, AC-005, BR-004 | ATEST-005, ITEST-004 | Developer | **proposed** |
| CHG-027 | 2026-08-16 | `api-specification.md` — `GET /scenarios/{scenario_id}`'s `forecast_revisions[]` gains **`ranked`**; `frontend-component-spec.md` (`ForecastRevisionControl`) — an unapplied revision is shown and **not offered** | TECH v3.12 → v3.13 (**proposed**) | **A forecast the prepared file carries and a revision that has been ranked are two different things, and one response reported them as one list — so the screen this task delivers could be broken by pressing a button it drew itself. Found at the review that blocked TASK-006, by the check for a described state with nowhere to live, and it is the observation the second review of TASK-005 made about `DispatchBoard` recurring one screen later.** CHG-025 put the series in `scenario_forecast_revisions`, written whole at load, so `GET /scenarios/{id}` answers `forecast_revisions: [0, 1, 2]` the moment a storm is loaded while only revision 0 has a ranking. Nothing in the response — and nothing in the schema — distinguished the two, and `ForecastRevisionControl` renders one selectable button per entry. Driven in a real browser: clicking **Revision 2** on a freshly loaded storm calls `GET /risks?forecast_revision=2`, which **correctly** answers 404 because `technical-spec.md` §7.3 forbids a silent fallback; `ScenarioView`'s single `catch` then put the whole screen into its error state. The ranking became *“We could not load the ranking… try again”*, which never succeeds; the asset table went with it; the control still read **Forecast revision 0 · current** with no indication of what had happened; and `RecommendationDecision` stayed on screen, still bound to revision 0's `recommendation_id`, offering *accept / change / reject* beside a ranking that was not there. **Two things are decided here.** **(a) The response says which revisions can be read back.** Each entry carries `ranked`, computed from `risk_scores` rather than inferred from `scenarios.forecast_revision`: the pointer is one number and the rankings are the fact, and the same review showed the two can disagree — a pointer moved directly to a revision nothing ranked leaves the default `GET /risks` answering 404 while every screen still reads *current*. **(b) The control offers only what can be answered.** An unapplied revision is rendered, disabled, and labelled *not applied yet, so there is no order to compare* — the rule the apply button already followed at the end of the series, applied to the list beside it. **Declined:** *hiding unapplied revisions altogether* — the operator most wants to know that the weather moves again at 12:00, and a list that silently shortens is the same class of screen as a board that silently drops work; *leaving the control alone and making `ScenarioView` retry or swallow the 404* — that hides a correct refusal behind a spinner and would show one ranking to a reader who asked for another the first time it half-worked; *making `GET /risks` fall back to the current revision* — §7.3 forbids exactly this and it is the failure ITEST-004 exists for; *deriving `ranked` from the pointer* — cheaper, wrong whenever the pointer and the rankings disagree, and mutation-checked as red. **One further change falls out of the same finding and is part of it:** `ScenarioView`'s three reads now settle independently, a ranking that could not be read is **cleared** rather than left standing beside a newer revision number, and `RecommendationDecision` is not rendered without the ranking it decides about — a decision is a decision about a list, and BR-001 means a person takes it while looking at one. Implemented by `store/forecasts.py`, `api/views.py`, `lib/api.ts`, `ForecastRevisionControl.tsx`, `ScenarioView.tsx`, and covered by `e2e/ATEST-005.spec.ts` — the browser case done criterion 12 did not have; **acceptance owed.** | REQ-F-004, AC-005, REQ-R-001 | ATEST-005, ITEST-004 | Developer | **proposed** |
| CHG-028 | 2026-08-16 | `database-design.md` §3 — `risk_scores` gains a `before delete` guard, a `before insert` guard that a ranking names one of its storm's forecasts, and the composite `(asset_id, scenario_id)` foreign key | TECH v3.12 → v3.13 (**proposed**) | **Three invariants the store could hold and did not, all on the one table AC-005 is about. Raised as observations at the review that blocked TASK-006 — none is reachable from caller input, which is what kept them observations rather than that log's pre-declared Block — and closed here rather than carried forward.** **(a) *Never rewrite an earlier revision. Not by `UPDATE`, not by delete-and-reinsert.*** TASK-006's own Constraints, and only the first half was in the schema. `delete from risk_scores where scenario_id = ? and forecast_revision = 0` issued directly against the database was **accepted**, and one re-insert rewrote revision 0 into a one-row list that `GET /risks?forecast_revision=0` then served with a **200** — the order a crew was placed against, changed underneath the decision that names it. Migration 010 declined a `before delete` twin **in writing**, because `risk_scores` is cascade-deleted with its scenario and with its assets and a delete guard would turn §7.2's *delete or replace a scenario* into an integrity error; the review called that a genuine trade-off honestly recorded rather than a slip, and it is right about an **unconditional** guard. What it missed is that the two cases are distinguishable inside the trigger: a cascade removes the parent row before applying the action to its children, so within one, a parent of the row being deleted is already gone. The guard fires only when **both** parents are still present. Both cascades — a whole scenario, and a single asset — are asserted by tests rather than reasoned about, because SQLite does not promise that ordering and a future version changing it should turn a test red rather than §7.2 red during an incident. **(b) A ranking could name a forecast revision its storm does not carry.** A row at revision 42 of a storm with three forecasts was accepted and served with a 200, and the pointer could be moved to 42 directly, after which the default `/risks` read is a 404. It matters more since CHG-027, which makes *which revisions have an order behind them* something a screen reads. **(c) `risk_scores.asset_id` proved existence and never membership.** CHG-019 recorded this exact instance as knowingly unfixed — *“rewriting a fourth table inside a TASK-005 remediation would be scope this task does not have”* — and it is closed here because this migration rebuilds the table anyway and `assets (id, scenario_id)` has been unique since 008. **(b) is a `before insert` trigger and not a foreign key, and the reason is evidence rather than preference.** `foreign key (scenario_id, forecast_revision) references scenario_forecast_revisions (…)` is the constraint the review named, and it was written, run, and **withdrawn**: with `on delete cascade` it hands `scenario_forecast_revisions` the power to delete rankings, and migration 010's *down* drops that table — so rolling 010 back destroys **every stored ranking in the database**, a rollback that silently empties the one table AC-005 exists to protect. With `on delete restrict` instead, §7.2's scenario delete becomes an integrity error, because a scenario reaches `risk_scores` by two paths and SQLite does not define which cascade it applies first — CHG-024's argument, unchanged. And the key cannot be satisfied by data that already exists after a 010 rollback: the rankings survive, the forecast times do not, and there is no honest `valid_time` left anywhere in the database for a revision above 0, so the rebuild would either abort the upgrade or invent a forecast time out of a computation time — a fabricated fact on the column BR-003 puts an age beside. The trigger is the narrower statement and the true one: **what may be written.** **Declined:** *leaving all three to the service layer* — that is the finding, and the standing Block condition; *`on delete no action` on the asset key* — CHG-024's reasoning; *a `before delete` guard with no `when` clause* — mutation-checked, and it turns both cascades into integrity errors. **A limit recorded rather than implied away:** the insert guard says no orphan can be **created**, not that none can **exist**, so deleting a `scenario_forecast_revisions` row directly leaves its rankings behind — and because that trigger reads a table 010 creates, **011 must be rolled back before 010 and not after**, which is stated in both down migrations and asserted in both directions by the AC13 test. Implemented by migration 011; **acceptance owed.** | REQ-F-004, REQ-F-009, AC-005, REQ-F-010 | ATEST-005, ITEST-004 | Developer | **proposed** |
| CHG-029 | 2026-08-16 | `database-design.md` §3 — what a `decision_records` row of kind `'placement'` contains, and a `before insert` trigger that refuses one the store cannot trace; `api-specification.md` — the request and response of `POST /scenarios/{scenario_id}/placements` | TECH v3.13 → v3.14 (**proposed**) | **An enumerated value with no writer, no reader and no shape. Found while preparing TASK-007, and it is CHG-021's finding one column over.** `decision_records.kind` has permitted `'placement'` since migration 006; nothing has ever written one, nothing has ever read one, and `§3` says `payload: json, required` and stops there. `product-spec.md` §10 is the only place the feature is written out and it makes a claim that has nowhere to live: a placement is *"saved, visible, and **traceable to the ranking and forecast revision it was made against**"*. Traceable is not a property of a response body — it is a property of the stored row, and `review-log.md`'s standing **Block** condition is *a rule enforced in the service layer that the store could refuse*, which has already fired on a foreign key that proved existence rather than membership (CHG-019) and on a `unique` constraint that could not see the normalisation in front of it (CHG-023). **Four things are decided here.** **(a) A placement is one `decision_records` row of kind `placement`, not a table of its own.** §3 already permits the kind and §10 says the flow *"writes one row to the decision record"*. **(b) Its subject is the ranking**: `subject_type = 'ranking'` and `subject_id = '<scenario_id>:<forecast_revision>'`, spelled **identically** to the `recommendation` row for that ranking, so migration 006's existing `decision_records_by_subject` index answers *what was recommended here, and what did people decide about it* in one lookup. **(c) Its payload is exactly `crew`, `asset_ids`, `forecast_revision`, `recommendation_id` and `note`.** The "where" is a list of **asset ids and nothing else** — §10 says *against named assets*, CON-003 forbids any premise-level record, and the consequence is that no address, coordinate or household has a field to arrive in rather than being stripped out of one on the way through. The "who" is a **crew display label**, which is the one piece of crew data CON-003 permits (it forbids *"crew personal data beyond a display name and role"*). `recommendation_id` is the delivered ranking when one has been delivered and `null` otherwise — reachable only outside the screens, because `PlacementForm` is not rendered without a ranking, and the subject names the ranking either way. **(d) The store refuses a placement it cannot trace.** `decision_records_placement_shape` fires only for `kind = 'placement'` and requires: a crew label of 1–120 characters, trimmed and free of control whitespace; between 1 and 500 assets, each named once; an integer `forecast_revision`; a `subject_id` that agrees with it; a note that is text of at most 2000 characters or null; and — the load-bearing clause — **every asset named is on the ranking that revision produced**, which is membership rather than existence, on caller-supplied input. An **UNSCORED** asset satisfies it: it has a `risk_scores` row, it is in the ranking and not ranked, and refusing to let a crew be placed at one would be the review log's first pre-declared Block condition wearing a validation rule's clothes. **It is a trigger and not a `check` constraint, and the reason is ADR-004 rather than preference.** SQLite cannot add a `check` to an existing table, so a constraint means rebuilding `decision_records` — `create table … new`, copy, `drop table`, rename — and dropping that table **drops both append-only triggers with it**, so the rebuild would have to create them again. ADR-004 forbids exactly that, CLAUDE.md lists it under *Never*, and migration 008 met the same wall when it added `seq` and used `alter table … add column` to stay clear of it. A `check` also cannot see another table, and the clause that matters is a join — the same argument CHG-026 and CHG-028(b) made. **Declined:** *a `placements` table with a composite foreign key into `risk_scores`* — it is the tighter constraint and ADR-002 would ordinarily prefer it, but it splits one decision across an append-only table and an ordinary one, so a placement's assets could be edited or deleted while the audit row naming them stayed put; BR-004 would hold over half a fact. *Rebuilding `decision_records` to add a `check`* — the Never rule above; the rule is not worth the mechanism that enforces it being taken down to install it. *Leaving the payload unconstrained and validating in `api/placements.py`* — that is the finding, and it is the Block condition. *`GET /api/v1/scenarios/{id}/placements`* — a read endpoint the index does not carry. CHG-016 added one and was justified by the feature being otherwise **unreachable**; this one is reachable and complete: the placement is returned by the write that made it, `PlacementForm` keeps it on screen, and an admin reads every one of them through `GET /scenarios/{id}/decisions`. `frontend-component-spec.md` gives `PlacementForm` the states *idle, validating, saving, success, error, permission denied* and no list state, so adding one would be inventing a screen. **It is the obvious next thing to want** — a non-admin cannot see yesterday's placements after a page reload — and it is named here rather than built, because that is a decision about what the planning view **is**. *A 409 on a second placement* — a ranking carries any number of placements, because several crews wait in several places; a decision is one per recommendation because a recommendation is accepted, changed or rejected once, and a placement is not a verdict on the list. *A crew roster table* — crews are a typed label, because a table of crews is crew data CON-003 has an opinion about and no requirement asks for. **One limit recorded rather than implied away, twice.** The trigger says what may be **written**, not what may **exist**, so a ranking deleted afterwards leaves a placement pointing at a list that is gone — nothing deletes a ranking except a scenario delete, which `decision_records` deliberately does not cascade with, because an audit row must outlive the thing it describes. And **012 must be rolled back before 011 and not after**: the trigger reads `risk_scores`, 011 rebuilds that table by dropping and renaming, and since SQLite 3.25 a rename reparses every trigger in the schema — so with 012 applied, 011 aborts inside a transaction that rolls back whole. That is the ordinary reverse order, it fails loudly rather than silently, and `test_TASK-007-AC10` asserts both directions. Implemented by migration 012, `store/decisions.py`, `store/rankings.py`, `api/placements.py`, `api/views.py`, `views/PlacementForm.tsx`; **acceptance owed.** | REQ-F-005, BR-001, CON-003, REQ-NF-007 | E2E-001, FTEST-005 | Developer | **proposed** |
| CHG-030 | 2026-08-16 | `api-specification.md` — one row added to the endpoint index | TECH v3.14 → v3.15 (**proposed**) | **Nothing lists the loaded storms, found while preparing TASK-009.** `frontend-component-spec.md` gives `ScenarioSwitcher` the purpose *“choose among the loaded storms”* and the data *“loaded scenarios: name, source note, loaded date”*; `AppShell` keeps it present always *“because everything below it is scoped to one scenario”*; `product-spec.md` §9's load flow promises the new storm *“appears in the list alongside any others already loaded, and becomes selectable”*; and CHG-001 settled that several storms coexist. The endpoint index carried `POST /api/v1/scenarios` and `GET /api/v1/scenarios/{scenario_id}` and **no collection read** — so the component whose whole purpose is choosing among loaded storms had no way to learn that more than one existed, and `ScenarioView` could only ever show the storm the browser had just uploaded. A page reload lost it. Adds **`GET /api/v1/scenarios`**, **signed in and not privileged**: `technical-spec.md` §7.2 gives *Switch between loaded scenarios — Admin yes, User yes*, and `database-design.md` §5 says a scenario is *written only by an admin, readable by every signed-in user*. Response: one entry per storm carrying `scenario_id`, `name`, `source_note`, `loaded_at`, `forecast_revision`, `forecast_issued_at`, `data_age_hours`, `stale`, `asset_count` and `ranked`. **`ranked` is CHG-027's argument one component over** — a storm whose current revision has no order behind it is one click from an empty screen, and an empty screen in this product reads as safety, so the response has to be able to say which is which. **Nothing finer than a count** (CON-003, REQ-NF-007): no asset id, no coordinate, no neighbourhood — and this is the cheapest place in the product for one to be added by accident, because a switcher row reading *“3 substations at risk”* would look helpful. Additive, which the versioning table calls a safe change, and **the same shape as CHG-009 and CHG-016**: an endpoint the build needed that the index did not carry. **Declined:** *returning the loaded storms inside `GET /api/v1/auth/session`* — it ties the list's freshness to the session read, so a storm loaded in another tab stays invisible until sign-out; *keeping the list in the browser* — a reload loses it, which is the defect, and `technical-spec.md` §3 forbids the browser holding anything the server must be authoritative about; *a `current_scenario_id` column per user* — a durable pointer to what somebody was last looking at is a new column and a new decision nothing asks for, recorded in TASK-009's *Out of scope* rather than taken silently. Implemented by `api/scenarios.py`, `store/scenarios.py`, `api/views.py`, `views/ScenarioSwitcher.tsx`, `views/AppShell.tsx`; **acceptance owed.** | REQ-F-010, REQ-R-001 | ITEST-005, STEST-001 | Developer | **proposed** |
| CHG-031 | 2026-08-16 | `database-design.md` §3 — `scenarios.content_key` added, with a unique index and two triggers; `source_note` gets its meaning back | TECH v3.15 → v3.16 (**proposed**) | **A column holding a SHA-256 digest under a name that means something else, found while preparing TASK-009.** §3 defines `source_note` as *“which prepared dataset this is, and where it came from”*, and `data-and-integration-spec.md` §3 has the admin supply it in the multipart body. §5 separately requires that *“loading a scenario whose content is identical to one already loaded replaces that one in place”* — and the digest that rule turns on **had no column of its own**, so `api/scenarios.py` wrote it into `source_note` and threw the admin's typed note away. `ScenarioSwitcher`'s specified third field was 64 characters of hex. **The worse half is where the rule lived.** *Identical content is one storm* was `find_by_content_key` — a `select` in front of an `insert`. A direct insert produced two rows for one upload, each with its own ranking of the same weather, and a decision recorded against one of them is a decision about a list the other reader never saw. That is ADR-002's exact prohibition — *never implement a check in the service layer that the store could refuse* — and `review-log.md`'s standing **Block** condition, reached for the third time by asking *what can a direct insert put in that column*. Adds **`content_key`** with **`unique`**, and a `before insert` trigger requiring it to be a 64-character lower-case hexadecimal digest. The case clause is not decoration: `hexdigest()` never produces upper case, so an upper-case key is a second spelling of one storm and `unique` cannot see past a spelling (CHG-023, one column over). The same trigger requires `name` and `source_note` to be non-blank and bounded, with the whitespace **enumerated** rather than left to SQLite's one-argument `trim()`, which strips spaces only; both bounds are tied to `store/scenarios.py` by a test that fails when they disagree, so the specified `400 validation_error` cannot silently become a `500`. A second trigger fixes a loaded storm's identity at load: the content key, the name and the source note are never rewritten. **`scenarios` is not rebuilt, and that is a decision rather than a convenience.** Eight tables reference it, six of them with `on delete cascade`, so the standard SQLite rebuild — create, copy, `drop table scenarios`, rename — would fire every cascade and empty the database on the way past. A `check` constraint is therefore unavailable and the rules are triggers: CHG-026, CHG-028(b) and CHG-029's argument reused — *a rule the schema cannot express without destroying something else is a trigger, and it says the true and narrower thing: what may be written.* **Declined:** *leaving §5's rule in the endpoint* — that is the finding, not a fix; *a `unique` index on `source_note` instead* — it would collide two storms because an admin typed the same note twice, and separate two loads of one file because they typed different ones; the note is prose, the digest is identity; *making `content_key` `not null` by `alter table`* — SQLite requires a default, and any default is a value two rows could share; *allowing a storm to be renamed* — nothing in the product renames one, and an identity that can move is an identity a switcher cannot be read against; *a `before delete` guard on `scenarios`* — CHG-024's reasoning, unchanged. **One consequence is recorded rather than implied away:** the up migration recovers each storm's typed note from the `scenario_uploads` row that recorded the upload, so the round trip is lossless and a roll-forward does not destroy a real fact by way of an ops procedure; rows loaded before 013 — whose note was never stored anywhere — get a sentence saying it was not recorded rather than an invented one, which is migration 010's loud-backfill rule applied again. Implemented by migration 013, `store/scenarios.py`, `api/scenarios.py`, `api/uploads.py`, `views/ScenarioUploadPanel.tsx`; **acceptance owed.** | REQ-F-010, REQ-F-001 | ITEST-005 | Developer | **proposed** |
| CHG-032 | 2026-08-16 | `database-design.md` §3 — a `seq` column on `scenarios` | TECH v3.16 → v3.17 (**proposed**) | **CHG-018's decision, on the fourth table — the one a person reads as a list.** CHG-018 gave `repair_jobs`, `damage_reports` and `decision_records` a monotonic `unique` `seq` because `datetime.now(UTC).isoformat()` resolves to about **15.6 ms** on this platform (1,999 of 2,000 consecutive calls return an identical string) and `id` is a random UUID, so `order by <timestamp>, id` is a coin flip for two rows written inside one tick. `scenarios` was left out of that change for a stated reason: nothing read it as a list. **TASK-009 is the task that reads it as a list**, and it is the only list in the product a person scans rather than a machine — so two storms loaded back to back appeared in an order that changed between reads. Adds `seq integer`, backfilled from the rowid the rows were already written in — so the order that existed by accident becomes the order that exists on purpose — allocated by the single writer inside the insert statement (ADR-002), and **`unique`**, so two storms claiming one place in the history are refused by the database rather than resolved by chance. `store/scenarios.all_loaded` reads `order by seq desc`. **No new decision is taken here**: this is the same decision applied where it was missed. It is a separate entry rather than a footnote to CHG-018 because that entry is proposed against three named tables, and quietly widening an entry already under review is the drift this register exists to catch. **Declined:** *`order by loaded_at desc, id`* — that is the defect; *a higher-resolution clock* — resolution is a property of the platform, and the next platform brings it back; *ordering by rowid directly* — it is not in the schema and `VACUUM` may renumber it, which is CHG-018's own reasoning. **Asserted against a fixture where the clock cannot help:** three storms are loaded with byte-identical `loaded_at` values, so nothing but a sequence can tell them apart, and the list is required to come back in load order every time. Implemented by migration 013 and `store/scenarios.py`; **acceptance owed.** | REQ-F-010 | ITEST-005 | Developer | **proposed** |
| CHG-033 | 2026-08-16 | `database-design.md` §3 — `damage_reports.dismissed_reason` must be a reason, and the dismissal check is given a name | TECH v3.17 → v3.18 (**proposed**) | **A reason that is not one satisfied `is not null`, found while preparing TASK-008.** §1 states REQ-F-008's rule in one line — *a dismissed report carries who dismissed it and why* — and §3 traces it to `check (status <> 'dismissed' or (dismissed_by is not null and dismissed_reason is not null))`. That constraint is real and it is only half the rule. Issued **directly against the database** beside a filed report, the store **accepted** `dismissed_reason = ''`, `'   '`, `char(9) || char(10)` and `'  padded  '`. The first three are an anonymous dismissal wearing a reason's clothes — which is exactly the risk `edge-cases-and-failures.md` records for UTEST-011, *an anonymous dismissal — control made cheap and untraceable* — and the fourth is a second spelling of one reason, which this repository has now paid for three times (CHG-023 on a location, CHG-031 on a content key). **It is the same hole in the same table, one column over.** CHG-023 found that `length(trim(…)) between 1 and 120` refused a spaces-only neighbourhood and **stored** a tab-and-newline one, because SQLite's one-argument `trim()` strips spaces and nothing else; `dismissed_reason` had no such clause at all, so every whitespace spelling got through. Fixed with the **two-argument `trim`**, whitespace enumerated, plus a lower and an upper bound: the only reason `damage_reports` will hold is one that has a character in it, is stored as it will be read, and is at most 2000 long. **A one-character reason is accepted and that is deliberate** — `unit-tests.md` says *brevity is not the rule*, and what the rule refuses is absence. **Two things fall out of the same change and are part of it.** *(a)* **The constraint is named.** `CHECK constraint failed: damage_reports` says a row was refused and not which rule refused it, and this table carries three checks that raise the same class; `review-log.md`'s lesson that *an assertion about a refusal has to name which refusal* is the same lesson one layer below HTTP, and naming the constraints is what lets a test obey it. `damage_reports_location_is_a_neighbourhood` is named in the same breath, unchanged otherwise. *(b)* **One bound, tied to its copy.** `length(dismissed_reason) <= 2000` is the number `store/dispatch.py:DISMISSAL_REASON_MAX` names, and UTEST-011 reads it back out of `sqlite_master` and requires the two to agree — leaving the schema at one number and the service at another is how the specified `400 validation_error` became a `500 internal_error` for a neighbourhood one character over the limit, with the whole suite green (CHG-023). It is written as `>= 1` and `<= 2000` rather than `between 1 and 2000`, which is migration 012's reason reused: UTEST-012 reads **every** `between 1 and N` out of this table and requires them all to be the neighbourhood bound, so a second `between` here would make that assertion unable to tell one bound from the other. 2000 is `decisions.NOTE_MAX`, not a new number — a dismissal reason is the same kind of thing a decision note is. **Declined:** *validating in `api/dismissals.py` only* — that is the finding, and `review-log.md`'s standing **Block** condition; *`length(trim(dismissed_reason)) >= 1`*, the obvious spelling — it is the clause CHG-023 proved wrong, and reusing it would reinstate a defect this log already records; *forbidding an internal newline the way `decisions.crew_label` does* — a crew label is a one-line name and a reason is somebody's sentence, so only the ends are governed; *making `dismissed_reason` `not null` outright* — §3 calls it optional and an open report has nothing to explain, so narrowing it would be a decision about what a damage report **is** (CHG-022 declined the parallel act on `repair_job_id`). **A limit recorded rather than implied away:** the check governs what a dismissal may hold, not whether it is true — `'x'` is a reason to the store and to nobody else. Implemented by migration 014; **acceptance owed.** | REQ-F-008, REQ-F-007, CON-003 | UTEST-011, UTEST-012 | Developer | **proposed** |
| CHG-034 | 2026-08-16 | `database-design.md` §3 — a `before update` trigger on `damage_reports` | TECH v3.18 → v3.19 (**proposed**) | **A dismissal could be rewritten, so *who dismissed it and why* meant *whoever wrote it last*. Found while building TASK-008, and it is CHG-026's finding one table over.** §1's rule for a damage report is *a dismissed report carries who dismissed it and why (REQ-F-008)*, and what the schema held was a constraint on the row's **shape** and nothing about its **history**. Issued directly against the database, `update damage_reports set dismissed_by = ?, dismissed_reason = 'never mind'` on an already-dismissed report was accepted — a different person's name and a different account of why, over the top of a decision somebody took during a storm — and `update … set status = 'open'` un-cleared the alarm while leaving the dismissal fields behind. The rule lived in the fact that no code issued the statement, and ADR-002 exists because a rule that lives only in code is removed by the first refactor with every test still green. Adds **`damage_reports_dismissal_is_final`**, a `before update` trigger that aborts when a row that was **already** dismissed changes its `status`, its `dismissed_by` or its `dismissed_reason` — the same shape ADR-004 uses for `decision_records` and CHG-026 uses for `risk_scores`, and asserted the same way: by issuing the statement against the database and requiring the refusal. **It is narrow on purpose, and the narrowness is asserted.** An open report can still be marked `duplicate`, which is CHG-021's state and now has a reader; a dismissed report's other columns are not frozen. A guard that fires on rows it was not written for is the same defect as one that does not fire at all and it is quieter — pointing the `when` clause at `old.status = 'open'` (present and wrong, rather than absent) turns **27** tests red, including the silent case that exists to catch exactly that. **Comparison is by `is not` rather than `<>`**: `null <> null` is null and a `where` clause that is null selects nothing, so the obvious spelling would accept exactly the row it was written to refuse — the trap migration 012's first clause already records. **Declined:** *an append-only pair on `damage_reports`* — the table is a working list and a report legitimately changes status while it is open; the append-only guarantee belongs to `decision_records`, which is where CHG-035 puts the dismissal's permanent record; *a check constraint* — a check cannot see `old`; *permitting an un-dismiss with its own actor and reason* — that is a second feature, nothing in `requirements.md` or `product-spec.md` asks for one, `agent-task-list.md` A-013 does not name it, and a control that can be undone silently is not the control REQ-F-008 describes. If a human decides a mistaken dismissal must be reversible, the answer is a new endpoint and a new `decision_records` kind, which is a decision about what the board **is**. **A limit recorded rather than implied away:** the trigger says a dismissal may not be **rewritten**, not that the report may not be deleted — `damage_reports` is cascade-deleted with its scenario and with its asset (CHG-024), and the audit row outlives both. Implemented by migration 014; **acceptance owed.** | REQ-F-008, REQ-F-009, BR-004 | UTEST-011 | Developer | **proposed** |
| CHG-035 | 2026-08-16 | `database-design.md` §3 — what a `decision_records` row of kind `'dismiss'` contains, and a `before insert` trigger that refuses one the store cannot trace; `api-specification.md` — the request and response of `POST /damage-reports/{report_id}/dismiss` | TECH v3.19 → v3.20 (**proposed**) | **An enumerated value with no writer, no reader and no shape — the third instance, after CHG-021 and CHG-029, and found by the same question.** `decision_records.kind` has permitted `'dismiss'` since migration 006; nothing has ever written one, nothing has ever read one, and §3 says `payload: json, required` and stops there. **AC-008 is unambiguous about which way it should be settled:** *given **any** recommendation or human decision, when it occurs, a row is appended carrying the timestamp and the acting user, and no path exists to edit or remove it.* Clearing a false alarm is a human decision — it is US-010, it takes work off a shared board during a storm, and `damage_reports` is **not** append-only. Without this row the only record of the decision lived in three mutable columns on a table that is deleted with its scenario, which is the audit gap BR-004 exists to close. **Four things are decided here.** **(a) A dismissal is one `decision_records` row of kind `dismiss`, not a table of its own** — §3 already permits the kind, and CHG-029 declined a `placements` table for the reason that applies unchanged: splitting one decision across an append-only table and an ordinary one lets half of it be edited. **(b) Its subject is the report**: `subject_type = 'damage_report'` and `subject_id` the report id, so migration 006's `decision_records_by_subject` index answers *what happened to this report* in one lookup. **(c) Its payload is exactly `reason`, `neighbourhood` and `repair_job_id`.** Nothing finer than a neighbourhood, because nothing finer is stored to copy (CON-003, REQ-NF-007), and nothing about a person beyond the actor the row already names. **(d) The store refuses a dismissal record it cannot trace.** `decision_records_dismiss_shape` fires only for `kind = 'dismiss'` and requires the subject to be a damage report and — the load-bearing clause — that the report named **exists in this storm, is dismissed, was dismissed by this actor, for this reason, in this neighbourhood, against this repair job**. That is membership rather than existence, on caller-supplied input, which is the distinction CHG-019 was blocked over and CHG-029's seventh clause turns on: a row claiming somebody dismissed a report that is still open is an audit trail asserting something that did not happen. **The payload repeats three facts the report already holds, and that is the decision rather than an oversight.** CHG-017 declined a display column on `repair_jobs` because two stored places to disagree about one fact is one too many, and the argument does not reach here for one reason: `decision_records` deliberately does not cascade with its scenario (migration 006, *an audit row must outlive the thing it describes*), so a row that only pointed at the report would say nothing at all once the storm was deleted. The trigger is what stops the copies ever disagreeing — they must be equal when the row is written, and neither can move afterwards: the report because of CHG-034, the record because of BR-004. **It is a trigger and not a `check`, and the reason is ADR-004 rather than preference** — SQLite cannot add a `check` to an existing table, so a constraint means rebuilding `decision_records`, and `drop table` takes **both append-only triggers** with it. CLAUDE.md lists that under *Never*. A `check` also cannot see another table, and the clause that matters is a join: CHG-026, CHG-028(b) and CHG-029's argument, unchanged. **The endpoint's shape is fixed here because `api-specification.md` says it should be** — the index carries the row and its *Endpoint template* section records that the remaining ten endpoints are specified *“when their task is written”*. Body: `reason`, required, 1–2000 characters, trimmed, every other field **refused** rather than dropped. `201` returning the stored report and the id of its audit row; `400` for a reason the store would refuse; `404` for an unknown report; **`409` for a second dismissal**, naming the first and leaving it byte-identical — BR-004's shape one table over, decided before the write so a retrying client cannot produce two audit rows for one human decision. **Declined:** *no decision record at all, on the ground that `damage_reports` already carries who and why* — those three columns were rewritable until CHG-034 and the report dies with its storm, so the only record of the decision would be neither append-only nor permanent; *`GET /api/v1/damage-reports/{id}`* — a read endpoint the index does not carry, and unlike CHG-016 the feature is complete without one: the dismissal is returned by the write that made it, the board reflects it, and an admin reads every one of them through `GET /scenarios/{id}/decisions`; *a `dismissed_at` column on `damage_reports`* — the audit row's `occurred_at` is the time, and a second copy of it is a second fact; *counting a dismissed report in `open_reports_in_area`* — it is not open work, and CHG-021 settled the parallel question for `duplicate`; *hiding a job whose reports have all been dismissed* — CHG-020 named that as TASK-008's decision to take, and it is taken here: **the job stays on the board**, reading *explained*, because quietly removing work from a shared dispatch board is the more dangerous of the two guesses. **One existing test changed and it is named rather than left to be noticed:** `test_TASK-007-AC4::test_the_placement_rules_leave_every_other_kind_alone` fabricated a `dismiss` row to prove the placement trigger's `when` clause does not fire on other kinds. The assertion is unchanged and now uses `reject`, because `dismiss` is no longer a kind without a guard. **A limit recorded rather than implied away, twice.** The trigger says what may be **written**, not what may **exist**, so a report deleted afterwards leaves its dismissal record behind — which is what an audit row is for. And because it reads `damage_reports`, **any future migration that rebuilds that table must drop this trigger first and recreate it afterwards**, and **014 must be rolled back before 009 and not after**: a rename reparses every trigger in the schema, so it would abort in the window where the table does not exist. That is the ordinary reverse order, it fails loudly rather than silently, and `test_TASK-008-AC9` asserts the rollback in both directions. Implemented by migration 014, `store/dispatch.py`, `store/decisions.py`, `api/dismissals.py`, `api/views.py`, `views/DismissAlarmControl.tsx`; **acceptance owed.** | REQ-F-008, REQ-F-009, AC-008, BR-001, BR-004, CON-003 | UTEST-011, ATEST-008 | Developer | **proposed** |
| CHG-036 | 2026-08-16 | `database-design.md` §3 — a partial `unique` index on `decision_records (subject_id) where kind = 'dismiss'` | TECH v3.20 → v3.21 (**proposed**) | **One human decision produced one audit row because two pieces of service code said so, and the store said nothing. Found at the review of TASK-008 and confirmed again at its re-review.** TASK-008's done criterion 7 is *“exactly one `decision_records` row of kind `dismiss` is appended”*, and `api/dismissals.py` states the reason in its own comment: the `409` is *“decided **before** the write so a retrying client cannot produce two audit rows for one human decision.”* The store held half of it and the wrong half. CHG-034's `damage_reports_dismissal_is_final` fires `when old.status = 'dismissed'` and aborts only when `status`, `dismissed_by` or `dismissed_reason` **change** — so a *different* second dismissal is refused and an **identical** one is not: the `update` changes nothing, the trigger stays quiet, and `append_dismissal` writes a second row that `decision_records_dismiss_shape` then accepts, because it agrees with the report in every particular. With the endpoint's `409` branch and `dismiss_report`'s `status <> ?` guard removed — the two service-layer copies, and nothing else touched — an identical retry was answered **`201` twice** and the table held **2** `dismiss` rows for one decision, with the whole gate green. That is `review-log.md`'s standing **Block** condition in its plainest form and ADR-002's prohibition verbatim: *never implement a check in the service layer that the store could refuse.* Adds **`decision_records_one_dismissal_per_report`**, a partial `unique` index. **It is an index and not a `check`, and that is ADR-004 rather than preference:** SQLite cannot add a `check` to an existing table, so a constraint means rebuilding `decision_records`, and `drop table` takes **both append-only triggers** with it — which CLAUDE.md lists under *Never*. An index creates no table and drops no trigger. Partial indexes have existed since SQLite 3.8.0; this platform is 3.49.1. **`subject_id` alone, not the pair, and the predicate is load-bearing.** A report id is unique across the database and `decision_records_dismiss_shape` already refuses a `dismiss` row whose subject is not a damage report, so the single column is both sufficient and stronger; `where kind = 'dismiss'` is what leaves every other kind alone, because a ranking carries any number of placements on purpose (CHG-029) and is re-read and re-recommended. **The refusal is identified by its own sentence** — `UNIQUE constraint failed: decision_records.subject_id` names this rule and no other, being the only unique constraint on that column and the only one on the table beside its primary key. **Declined:** *leaving it in `api/dismissals.py` and `dismiss_report`* — that is the finding; *widening `damage_reports_dismissal_is_final` to abort on any update of a dismissed row* — it would refuse the rows CHG-034 deliberately leaves movable and turn its silent case red, and it would still be a rule about the report rather than about the audit row; *an `on conflict do nothing` in `append_dismissal`* — a writer that swallows a duplicate is a writer that cannot tell a retry from a bug, and the endpoint's `409` is the answer the contract specifies; *rebuilding `decision_records` to carry a `check`* — forbidden outright, above. **One existing test changed and it is named rather than left to be noticed:** `test_the_store_accepts_a_dismissal_record_that_agrees_with_its_report` dismissed its report **through the endpoint** and then inserted a second audit row directly, asserting `len(rows) == 2` — so the store's acceptance of two audit rows for one decision had become a property the suite *required*, and the review recorded that whoever fixed this needed to know before starting. Its report is now dismissed by direct statement, which writes no audit row, so the permitted control is the only one and the assertion is `== 1`. Nothing about what the control proves has changed: it is still the accepted shape that keeps the six refusals below it from being satisfied by a trigger that refuses everything. **A limit recorded rather than implied away:** the index says what may be **written**, so a database carrying two `dismiss` rows for one report — reachable only by a direct insert while 014 was rolled back — makes migration 015 abort rather than being silently deduplicated. BR-004 means nothing may go back and tidy such rows; a person has to look. Implemented by migration 015; **acceptance owed.** | REQ-F-008, REQ-F-009, AC-008, BR-004 | UTEST-011 | Developer | **proposed** |
| CHG-037 | 2026-08-16 | `database-design.md` §3 — one alphabet for what counts as whitespace in `damage_reports.dismissed_reason` and `damage_reports.location`, tied across the schema, `store/dispatch.py` and the browser | TECH v3.21 → v3.22 (**proposed**) | **CHG-023's own sentence for the third time — *the same non-answer wearing a different whitespace character* — on the very column CHG-033 was written to close, and this one needed no mutation to find.** What counts as whitespace was written three times and the three disagreed: migration 014's checks enumerate six ASCII characters (`' ' || char(9) || char(10) || char(11) || char(12) || char(13)`), `store/dispatch.WHITESPACE` repeats the same six, and the browser uses JavaScript's `String.prototype.trim()`, which is Unicode-aware. On an untouched tree, `POST /api/v1/damage-reports/{id}/dismiss` with a reason of U+00A0, U+2003, U+200B or U+FEFF was answered **`201`** and that single invisible character was what `dismissed_reason` and the audit row then held, under a dispatcher's name, while `'   '` was refused. **It was invisible on screen because the strictest of the three definitions was the browser's** — the button stayed disabled — so the enforcement had ended up in the one layer ADR-002 says it must never live in, and only a caller reaching the API ever met it. **The alphabet is enumerated, and enumeration is the decision.** SQLite has no notion of Unicode whitespace: `trim(X, Y)` can only be given the characters to remove. Python's `str.split()`, JavaScript's `trim()` and Unicode's `White_Space` property are three different sets, so a rule written as *whatever this language calls blank* is three rules. `dispatch.BLANK_CODEPOINTS` is the one list — the six ASCII characters, the four information separators `str.split()` already collapsed, Unicode's `White_Space` additions, and the two invisibles that are **not** `White_Space` and are exactly what a caller reaches for: U+200B ZERO WIDTH SPACE and U+FEFF ZERO WIDTH NO-BREAK SPACE. It is repeated in the schema as `char(...)` and in `frontend/lib/dismissal.ts`, and `test_one_alphabet_decides_what_is_blank_in_every_layer` reads all three and requires them identical — *a rule written in more than one place needs something that fails when the copies disagree*, which `AGENT.md` already says about a bound and which is just as true of an alphabet. **The same hole sat in `damage_reports_location_is_a_neighbourhood` and is closed in the same breath.** Every clause of it accepted a neighbourhood of one no-break space when the JSON was written as raw UTF-8; it was unreachable only because `store/dispatch.py` calls `json.dumps` with `ensure_ascii` at its default, so the escaped character tripped the *unrelated* `json(location) = json_object(...)` clause instead. CON-003's guard against *a location that is not a place* was being held up by a serialiser default, and the day a neighbourhood needs an accent that default changes. The five `instr(..., char(n)) = 0` clauses become one `not glob '*[' || char(...) || ']*'` over the alphabet **without** the space — the same claim, *no blank character other than a single interior space*, over thirty characters instead of five. `dispatch.normalise` collapses runs of the same alphabet, so the endpoint still produces the only form the constraint accepts and a neighbourhood of one invisible character is the specified `400 validation_error` rather than the `500` it used to be. **And the bound's third and fourth copies are tied at last.** `DISMISSAL_REASON_MAX` was tied between the schema and `dispatch.DISMISSAL_REASON_MAX`, had a third copy in `frontend/lib/api.ts` described in its own comment as *mirrored* with nothing mirroring it, and a fourth as the literal `'N'.repeat(2001)` in `e2e/TASK-008.spec.ts`. Set the third to `8` and `tsc`, `lint`, `build` and all 36 browser specs passed while the field stopped a dispatcher at eight characters — and the one browser case that exercises the bound calls `input.removeAttribute('maxlength')` before typing, so it could not see the change by construction. The number and the alphabet now have **one** definition in the browser half, `frontend/lib/dismissal.ts`, which `lib/api.ts` re-exports and the browser case imports; a test enumerates every `.ts`/`.tsx` file under `frontend/` and fails if either acquires a second definition or stops matching the server's. **Declined:** *keeping `String.prototype.trim()` in the browser* — it is a fourth definition, and it is the one that hid the hole by being strictest; *serving the bound and the alphabet from an endpoint* — a new field in a response so a field can size itself is a contract change for a courtesy, and the mirror plus a test that fails when it drifts is what `AGENT.md` actually asks for; *`ensure_ascii=False` in `store/dispatch.py`* — it is the tidy-up that makes the location hole reachable, and doing it inside a remediation for something else is how a defect arrives wearing a cleanup's clothes (CHG-024's reasoning); the constraint is fixed **first** and the serialiser is left alone; *governing the interior of a dismissal reason* — a reason is somebody's sentence, so only its ends are governed, which is CHG-033's decision unchanged. **A limit recorded rather than implied away:** the alphabet is a list, and a list is a thing Unicode can add to. It closes the characters a caller reaches for; it does not make *invisible* a decidable property in SQLite. `store/scenarios.py`'s own six-ASCII copy (CHG-031, `name` and `source_note`) has the same hole and is **not** changed here, because it belongs to TASK-009 and widening an entry already under review is the drift this register exists to catch. Implemented by migration 015, `store/dispatch.py`, `frontend/lib/dismissal.ts`, `frontend/lib/api.ts`, `views/DismissAlarmControl.tsx`; **acceptance owed.** | REQ-F-008, REQ-F-007, CON-003, REQ-NF-007 | UTEST-011, UTEST-012 | Developer | **proposed** |
| CHG-038 | 2026-08-16 | `fitness-functions.md` — FF-003's `Runs` and `On failure` cells, and the paragraph that said it could not be wired | TECH v3.22 → v3.23 (**proposed**) | **FF-003 is wired, and wiring it required deciding three things the register does not decide.** TASK-010 is the task that wires the last fitness function, and the register is explicit that the `Runs` column is *a claim about a gate that has to EXIST* — which cuts both ways: `Not wired yet` beside a check that now blocks a merge is the same file lying in the other direction. The row is edited here rather than in the task, because nothing under `01-docs/` is an output of a task. **(a) What *open every screen* is, under ADR-008.** Every one of the sixteen views is `'use client'` and reaches data only through `lib/api.ts`, so a screen's content is entirely the responses behind it; the check drives **every GET the application serves**, discovered from the routing table rather than listed, so a screen a later task adds is covered without anyone remembering to add it. *Declined: driving Playwright from inside `ci/fitness.py`* — the browser suite is already a gate stage, it needs both processes and `workers: 1` since the race TASK-008 found, and five files × two states × a browser is the cost that gets a fitness function folded back into the test stage, which is the decay stage 4 exists to prevent. The browser half of (c) is a source scan instead, which is FF-002(b)'s shape for FF-002(b)'s reason. **(b) What *reads a source file* means: an `open`, not a `stat`** — and this one is a genuine contradiction in the register if it is left undecided. `views.integrity()` calls `is_file()` on all five source files on the render path of `GET /scenarios/{id}`; under the most literal reading of clause (c) those five calls are the violation, and clause (c) then forbids what clause (b) requires, because *the loss is named to an admin* cannot be answered without looking. The decision: **(c) forbids a source file's CONTENTS reaching a response** — an `open`, by any of the three doors Python has (`builtins.open`, `io.open`, `os.open`) — and asking whether a file exists is what (b) is. *Declined: any filesystem call counts* — it makes (b) and (c) contradict, and a check whose two clauses cannot both hold is not a gate. *Declined: moving the presence check off the render path* onto a schedule with its answer stored — defensible, and a bigger change than this task: it needs a column, a period, and a decision about how stale a *presence* answer may be, and CHG-013 put the notice in the scenario response deliberately. **A limit recorded rather than implied away:** five `is_file()` calls happen per read of that screen, and a reader who wants (c) to mean *nothing touches the filesystem on a render path* is asking for that declined option, not for a stricter check. **(c) How clause (a) is MEASURED**, which CHG-013 named as weak and did not replace. Its original sense — a screen breaks because it cannot read a file — still cannot happen. What is checked instead is the opposite claim, and it is the one CHG-013 actually decided: with a file removed, every screen read still answers `200`, still states its data's age, and **answers the same thing it answered before the loss**. That is failable today, in the direction the failure would really come from — `if not integrity["intact"]: rows = []` is one line, it is the tidy-looking wrong answer, and with it in the ranking read all **534** tests pass while a lost `outages.csv` empties the risk list, which is the empty screen CLAUDE.md forbids reading as safety. **Every clause was mutation-checked before this row was written**, and each mutation was applied, run, reverted, and the tree checked clean: (c) frontend — `fs.readFileSync` in `app/page.tsx`, a server component, with `tsc`, `lint` and `build` all **green**; (c) backend — `integrity()` reading `manifest.json` to report its size, with all **534** tests green; (b) — the missing-file list emptied, which FTEST-002 also catches, three of its cases; (a) — the ranking and then the asset table emptied on a lost file, the first invisible to the whole suite. **And the two ways this check could pass for want of anything to test are themselves guarded and themselves mutation-checked:** the storm's five files must be on disk before their absence from the reads means anything (deleting them after upload makes the gate report the vacuity rather than pass), and the recorder is shown a real source file through all three doors before its silence is believed (unwatching one door makes it report `2 of its own 3 canary reads`). The route walk carries the same discipline: this FastAPI wraps `include_router` in a `_IncludedRouter` whose own `path` is `None`, so a flat walk finds the four documentation routes and **none** of the seventeen endpoints — `AGENT.md`'s 2026-08-16 lesson, which caught this check while it was being written, and five named routes must now be in what the walk found. Implemented by `ci/fitness.py`; **acceptance owed.** | REQ-NF-003, AC-002, AC-010, REQ-F-001 | FF-003, FTEST-002, FTEST-003, ATEST-010 | Developer | **proposed** |
| CHG-039 | 2026-08-16 | `database-design.md` §3 — the blank alphabet governing `decision_records.payload.$.crew`, `scenarios.name` and `scenarios.source_note`; `store/blanks.py` (new); `frontend/lib/blank.ts` (new) | TECH v3.10 → v3.11 (**proposed**) | **One alphabet was decided and three columns never got it — and both defects were live on an untouched tree, with no mutation required.** Found at the round that reviewed TASK-007 and TASK-009 against their own done criteria for the first time. **(a)** `POST /api/v1/scenarios/{id}/placements` with a crew of one **U+200B ZERO WIDTH SPACE** or one **U+FEFF** was answered **201**, and that single invisible character is what `decision_records.payload` then held — a crew placement recorded under a person's name, naming a crew nobody can see, in the one table BR-004 forbids correcting. A correction is a new row, so the invisible one stays in the artefact `technical-spec.md` calls *what is produced to a regulator afterwards*, for ever. **(b)** `POST /api/v1/scenarios` with a `name` of one **U+00A0** was answered **201** and stored, and `ScenarioSwitcher` — whose entire purpose under REQ-F-010 is letting a person pick one storm out of several — drew a row with no visible label; the same for `source_note`, which §3 requires to say *which prepared dataset this is and where it came from*. `'   '` was refused at all three. **CHG-023's own sentence for the fourth and fifth time** — *the same non-answer wearing a different whitespace character* — and the second and third instances that needed no mutation to find. **Why it recurred after CHG-037 answered it.** CHG-037 was right about the alphabet and its reach was one module: it tied `store/dispatch.py`, migration 015 and the browser together for a dismissal reason and a neighbourhood, and tied nothing to any other column. Every column written afterwards reached for its own language's idea of blank, and the three languages are three different sets — Python's `str.strip()` removes `White_Space` and neither invisible; JavaScript's `String.prototype.trim()` removes U+FEFF and not U+200B; SQLite's one-argument `trim()` removes spaces alone. `store/decisions.crew_label` used the first, migration 012 used the third plus a five-way `replace` chain, `store/scenarios.py` and migration 013 both enumerated the same six ASCII characters — so **those two layers agreed with each other perfectly and were wrong the same way**, which is why nothing was ever red. That is `AGENT.md`'s standing observation, paid for again: *a rule that has to be re-derived at each boundary is a rule that will be missed at one.* **And the browser was the strictest layer in both cases**, which is the CHG-037 shape verbatim and the reason neither hole was visible from any screen: `PlacementForm` and `ScenarioUploadPanel` both used `String.prototype.trim()`, so the button behaved and only a caller reaching the API met the defect. **What is decided.** The alphabet moves out of `store/dispatch.py` into `store/blanks.py`, a **leaf** module — `dispatch` imports `decisions`, so `decisions` cannot import `dispatch` back without the cycle FF-001 refuses — and out of `frontend/lib/dismissal.ts` into `frontend/lib/blank.ts`, because it was never a fact about dismissals and being filed under one field's name is exactly what let two other fields keep their own definition. `dispatch.BLANK_CODEPOINTS` stays bound to the same tuple, so CHG-037's tie test is unchanged in what it asserts; its regex is **widened** from `DISMISSAL_BLANK_CODEPOINTS` to the suffix, which still matches the old spelling and still refuses a second home. Migration **016** puts the same 31 codepoints into `decision_records_placement_shape` and `scenarios_identity_shape` as `char(...)`. **DECLINED, in writing:** *widening `str.strip()` and `trim()` rather than enumerating* — that is the defect; a rule written as *whatever this language calls blank* is three rules. *Refusing the characters anywhere in the value rather than at its ends* — a crew label and a storm name may legitimately be written in a language whose spaces are not U+0020; what is refused is a value made of nothing, never a character, and each group of refusals ships with the accepted case that says so. *Fixing only the crew label* — `decision_records` is the worse of the two because it cannot be corrected, but `scenarios.name` is the one on the screen the requirement is about. *A `check` constraint on `decision_records` instead of a trigger* — it cannot be added without rebuilding the table, which drops both append-only triggers; ADR-004 forbids it and CHG-029 already routed around it. **NOT FIXED HERE AND RECORDED RATHER THAN LEFT SILENT:** `api/recommendations.py` does `(body.note or "").strip()`, so a `change` or `reject` whose **required** justification is one U+200B is accepted and written to `decision_records` — the identical shape on TASK-004's column. TASK-004 is **Done**, and CHG-024's rule is that an observation is recorded with its reasons rather than smuggled into a remediation for something else. `DispatchBoard`'s `neighbourhood.trim()` is a second definition too, and is left because the asymmetry runs the safe way there — the server already refuses more than the browser does. Implemented by migration 016, `store/blanks.py`, `frontend/lib/blank.ts`; **acceptance owed.** | REQ-F-005, REQ-F-010, BR-001, BR-004, CON-003 | E2E-001, ITEST-005, UTEST-011 | Developer | **proposed** |
| CHG-040 | 2026-08-16 | `constraints-and-non-goals.md` — the automatic summary writer moved from non-goal to scope; `ai-boundary-spec.md`; `requirements.md` — REQ-F-011; `api-specification.md` — three endpoints | TECH v3.23 → v3.24 (**proposed**) | **A non-goal the client has decided to build, and it is the widest surface a model has ever been given in this system.** `constraints-and-non-goals.md` lists *an automatic summary writer* as out of scope for version one — deferred rather than rejected — and the client's written build prompt now specifies it in full, with a Draft / Approved / Sent lifecycle and a human review step. ADR-009 permits the model to **phrase reasons the scorer computed** and to invent nothing; a paragraph of free prose is a far wider surface than a reason sentence, so the scope change ships **with** its guardrail rather than ahead of it. **The guardrail is code, and that is the decision.** After the model returns, and before anything is shown, the server extracts every numeric figure and every proper noun from the draft and refuses any that is not in the supplied set; scans for a forbidden vocabulary (`telemetry`, `sensor`, `predictive`, `model`, `landfall`, `sustained winds`, `mph`, `protocol`, `activated`, `AI`, `algorithm`); regenerates **once** on any violation; and on a second failure assembles the text from the figures directly and labels it *Assembled from platform data* rather than *Drafted from platform data*, so the reader can tell which they are holding. The verification result is stored on the row and rendered in the review drawer, listing **every** extracted figure rather than a fixed few, and any mismatch **disables** approval with the reason shown — a block, not a warning. Only the figures reach the prompt: open incidents, critical facilities affected, crews deployed and total, customers out, high-risk count, the top asset's name and impact, the forecast timestamp. **One of those widens ADR-009 and the widening is stated rather than slipped:** ADR-009's prompt rule was *only factor names and contributions — never an asset name*; the client's build prompt specifies the top-ranked asset's name in the summary figure set, and this entry records that decision for the summary path **only** — the reason-phrasing path keeps ADR-009's rule unchanged. Approval is recorded on the summary row itself — approver and time — for CHG-048's structural reason: the audit table's `kind` alphabet is frozen, and an approved summary names its approver either way. **Declined:** *bounding it with prompt instructions alone* — a model asked politely not to invent things still invents things, and the three failures the client's own testing recorded (*"eastern seaboard"*, *"sustained winds of 115mph"*, *"based on predictive structural models"*) are exactly what a polite instruction does not stop; *leaving it a non-goal* — the client has decided, and recording the decision is what this register is for; *auto-sending an approved summary* — BR-001 is unchanged, a person approves and a person sends; *letting the client call the model* — the key would be in a browser. | REQ-F-011, BR-001, REQ-NF-003 | EVAL-002, UTEST-013, ATEST-012 | Developer | **proposed** |
| CHG-041 | 2026-08-16 | `frontend-component-spec.md` — a map panel on the planning screen; `constraints-and-non-goals.md` — CON-006 amended: the Google Maps JavaScript API is the **second paid third-party service**, taken as its own decision exactly as that row requires | TECH v3.24 → v3.25 (**proposed**) | **A map, and the two rules that decide what may be on it.** The design places an asset map beside the ranked table and the client has chosen the Google Maps JavaScript API to draw it. **CON-003 and REQ-NF-007 are unaffected, and the reason is worth stating precisely rather than assumed:** they govern *damage* locations, which this platform holds at neighbourhood resolution and can hold at no other (CHG-017 put that in the schema). An **asset** coordinate is different — `assets.lat` / `assets.lon` have been columns since migration 002, they come from the utility's own registry, and CON-003 never forbade them. So the panel plots assets and **never** plots a damage report. **What leaves the browser is recorded rather than glossed:** tile requests carry the **viewport bounds** to Google; marker positions are placed client-side and the asset list is never sent. That is a real disclosure — the region a utility is looking at during a storm — and it is the client's call, taken knowingly. The panel degrades rather than breaks: with no `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY` configured it renders a plain SVG scatter of projected coordinates, so the screen is complete before the key exists and improves when it arrives. **Declined:** *a storm track, cone, wind or rainfall overlay* — the product is not a weather tracker and the build prompt forbids each by name; *a legend* — the dots are graduated by band and the band is already a word in the table beside them; *plotting damage reports* — there is nothing finer than a neighbourhood to plot, and inventing a centroid would manufacture a precision the record does not have; *a bundled basemap image* — it fixes one geography into the product, and the next scenario's coordinates would not be on it. | REQ-F-003, CON-003, REQ-NF-007 | ATEST-013 | Developer | **proposed** |
| CHG-042 | 2026-08-16 | `ADR-007`'s flood-zone table as implemented in `scoring/references.py`; `failure-tests.md` FTEST-004 | TECH v3.25 → v3.26 (**proposed**) | **An unrecognised flood zone stops making the asset unscorable and starts scoring at the minimum with a reason that names it.** Today a zone outside `{VE, AE, X}` makes the whole asset UNSCORED (FTEST-004), on the ground that guessing an exposure is how a coastal asset gets ranked like an inland one. That reasoning is right about *guessing* and wrong about *this*: FEMA zone D means **"flood hazard undetermined"**, and a handful of real assets carry it. Discarding three known factors — gust, age, condition — because the fourth is undetermined loses more information than it protects, and it puts a real asset into the unscored group where a reader has to work out why. **The replacement keeps the safety and drops the cost:** the zone scores at the **minimal-risk value** and the ranking carries a reason saying, in words, *the zone code was not recognised and was scored at minimal risk*. The asset is still ranked on everything else, and nobody reads *minimal flood risk* where the truth is *unknown flood risk*, because the sentence says which one it is. **This survives FF-007 unchanged** — the reason names factor `flood_zone`, which is already in `WEIGHTS`, so the gate that requires every reason to name a real factor and claim the strength it earned still holds. **Declined:** *keeping UNSCORED* — it is the current behaviour and it is the finding; *scoring at the AE value* — that is the guess the original rule was written against; *dropping the factor for that asset and re-weighting the other three* — the score would then not be comparable with any other asset's, which is the one thing a ranking needs; *silence* — an asset that says *minimal flood risk* when the truth is *nobody has determined it* is the confidently-wrong output ADR-005 exists to prevent. | REQ-F-002, REQ-F-003, BR-002 | FTEST-004, UTEST-009, EVAL-001 | Developer | **proposed** |
| CHG-043 | 2026-08-16 | `scoring/references.py` — `FLOOD_ZONE_EXPOSURE` gains A, AO and AH | TECH v3.26 → v3.27 (**proposed**) | **Three FEMA zone codes that mean the same hazard as one already in the table.** `FLOOD_ZONE_EXPOSURE` carries `VE`, `AE` and `X`. FEMA's A-prefixed zones — **A** (1% annual chance, no base flood elevation determined), **AO** (sheet flow) and **AH** (shallow ponding) — are all within the same 1%-annual-chance floodplain as **AE**; the letters record how the elevation was derived, not a different exposure. Without them, an asset in zone AO fell through to CHG-042's unrecognised path and scored at the minimum, which is the opposite of the truth: AO is a *high*-hazard zone. Each maps to the AE value. **Declined:** *a separate value per suffix* — the difference between AE and AO is surveying method, and inventing a fourth exposure number for it would be four uncalibrated numbers where there was one; *treating the bare A as unrecognised because it lacks a base flood elevation* — the hazard is determined even where the elevation is not, and that is precisely what distinguishes it from zone D. | REQ-F-002, REQ-F-003 | UTEST-009, FTEST-004 | Developer | **proposed** |
| CHG-044 | 2026-08-16 | `database-design.md` §3 — `horizon_hours` on `risk_scores`; `requirements.md` AC-005's neighbour | TECH v3.27 → v3.28 (**proposed**) | **Rank movement has to be real, and there was nothing for it to be real against.** The design's *Since you last looked* strip names four assets that moved and by how much, and the client's prompt is explicit that the delta is computed and never faked. REQ-F-004's forecast revisions **could** supply one, but only after a person presses *Apply the next forecast change* — so a freshly loaded storm has exactly one order and the strip has nothing to say, on the screen where it is most wanted. **The movement is the diff between two delivered rankings — revision n against revision n−1 — computed from stored `risk_scores` rows when a forecast change is applied, and stored in a new `rank_movement` table: per revision, appended, never rewritten,** each row naming the asset, both ranks, and the factor whose stored contribution grew, which is the reason it moved. At revision 0 there is no earlier order, **and the strip says so** — *first ranking for this storm, nothing to compare yet* — because the alternative was considered and found dishonest twice over: a second not-delivered scoring pass over a 48-hour window produces arrows that disagree with the ranks beside them (the table shows revision ranks, the arrows would show window deltas — one screen, two arithmetics), and FF-005's *one recommendation row per delivered ranking* starts needing an exception for a pass that exists only to decorate a strip. A diff of two stored rankings needs no second scorer, no second code path, and nothing that is not already on disk — the same property AC-005's comparison already has. **The design's headline claims a 72-hour horizon; the arithmetic has no horizon parameter, and the headline is worded from what the arithmetic actually is** (*at the current forecast*) rather than the copy — a number on screen that the computation cannot defend is the exact failure ADR-005 exists to prevent. **Additive on purpose.** REQ-F-004's file-driven revisions are untouched, `ForecastRevisionControl` keeps every behaviour CHG-027 decided, and the 36 browser cases that defend them stay green. **Declined:** *replacing forecast revisions with horizons* — it would delete AC-005's comparison and CHG-027's decision to close a live defect, in order to add a strip; *faking the delta from a single run* — the client's prompt forbids it by name and it would be a number nobody could defend; *computing the delta at read time* — `technical-spec.md` §6 requires every read to be served from stored results, and FF-003 fails a response that moves between two reads of the same storm. | REQ-F-004, REQ-F-003, AC-005 | ATEST-005, UTEST-014, FF-003 | Developer | **proposed** |
| CHG-045 | 2026-08-16 | `security-specification.md` §7 and the RBAC matrix; `database-design.md` §3 — the `users.role` check constraint | TECH v3.28 → v3.29 (**proposed**) | **The second role is renamed `user` → `operator`.** The two roles have been `admin` and `user` since migration 001. The client's build prompt names them `admin` and `operator` and freezes the vocabulary in the type system, and `operator` is also what every other document in this specification calls the person: `intent.md`, `product-spec.md` and the operator sessions ADR-007's calibration depends on. `user` was never a role, it was the absence of one. A migration rewrites the check constraint and the existing rows. **Declined:** *keeping `user` in the store and rendering "Operator" on screen* — a label that disagrees with the value behind it is a second definition, which this register has now paid for five times (CHG-023, CHG-037, CHG-039); *adding `operator` as a third role* — two roles is a decision, not an accident, and a third that means the same as the second is how an RBAC matrix stops being checkable. | SEC-A-004, REQ-R-001, REQ-NF-002 | STEST-003, STEST-004 | Developer | **proposed** |
| CHG-046 | 2026-08-16 | `database-design.md` §3 — `security_log` added | TECH v3.29 → v3.30 (**proposed**) | **The table CHG-015 decided and nothing built.** CHG-015 fixed AC-009's wording so that a refused upload is recorded in the **security log** rather than in `decision_records`, on the ground that *the decision record holds decisions about recommendations, and a refused upload is an access-control event, not a decision* — and declined a nullable `scenario_id` because the not-null constraint is part of what makes the audit table trustworthy. That decision was accepted by the client and the table was never added; the events have been going to the application log, which is not queryable, not retained on the same terms, and not readable by the *Recent activity* rail the design puts on the dispatch board. Adds `security_log`: actor, event, detail, time. Sign-in, sign-out, refused upload and permission denial are written to it. **It is not append-only by trigger and that is deliberate** — ADR-004's guarantee is about the regulatory record, `decision_records`, and extending trigger machinery to a second table would dilute what FF-004 proves about the first. **Declined:** *folding these into `decision_records`* — CHG-015 declined it with reasons that have not changed; *leaving them in the application log* — the rail cannot read it, and *"recorded"* in AC-009 has to mean somewhere a person can look. | REQ-R-001, SEC-Z-002, AC-009 | STEST-005 | Developer | **proposed** |
| CHG-047 | 2026-08-16 | `database-design.md` §3 — `data_findings` added; `api-specification.md` — `GET /scenarios/{id}/findings` | TECH v3.30 → v3.31 (**proposed**) | **Seven defect rules that run, are named, and then vanish.** `loader/defects.py` catches all seven and FF-006 gates on it; the findings travel out of `run_parse` into the **log** and into the upload response, and nowhere else. So the data-quality summary the design puts on the load screen can only exist in the seconds after an upload, and reopening the storm tomorrow shows nothing — while the defects are still in the data and still shaping the ranking. Adds `data_findings`, written in the same transaction as the scenario, and a read endpoint. **The screen's rule is part of this entry**, because it is what the table is shaped for: at most **three** findings that need a human decision are visible, each with one action and each naming the file the parse actually reported it against; everything auto-resolved collapses behind *{n} issues handled automatically*. A wall of warnings is how the three that matter get missed. **Declined:** *re-parsing on demand to regenerate them* — `technical-spec.md` §6 forbids computing a read, and FF-003 removes and corrupts the source files precisely to prove no screen depends on them; *hard-coding the affected file per defect rule* — defect 3 is measured in whichever file carries the gusts, and a rule that names a file it did not read is the wrong answer the moment a scenario is shaped differently. | REQ-NF-003, REQ-F-001, AC-001 | FTEST-001, UTEST-006, FF-006 | Developer | **proposed** |
| CHG-048 | 2026-08-16 | `database-design.md` §3 — `asset_match_candidates` added; `api-specification.md` — two endpoints | TECH v3.31 → v3.32 (**proposed**) | **The withheld merge is decided and then thrown away.** `loader/matching.py` implements AC-001's hard half exactly: where type and position agree and the name does not, it **withholds** the merge and marks the asset `needs_review`, on the recorded ground that a wrong merge deletes an asset from the ranking invisibly while a wrong split costs ten seconds in a queue. The queue does not exist. `assets.match_status` records *that* a record needs review and nothing about **what it would have been merged with**, so the review drawer the design specifies — two comparison cards, the differing fields tinted, *Match* and *Not a match* — has no data behind it and a person cannot resolve what the loader deferred to them. Adds `asset_match_candidates`, holding both sides of each withheld pair and its resolution, written at load and read by the drawer. **The resolution is recorded on the candidate row itself — actor, decision, time — and not in `decision_records`, and the reason is structural rather than preference:** the audit table's `kind` alphabet is frozen in a check constraint that cannot be extended without rebuilding the table, and CHG-036 already ruled the rebuild out because `drop table` takes both append-only triggers with it. CHG-015's line governs here too — the decision record holds decisions about **recommendations**, and deciding that two source rows describe one asset is a decision about data identity. **Confidence is stated in words and never as a percentage** — the loader's rule is a threshold on position and a comparison of names, and dressing that as *87%* would invent a precision the arithmetic does not have. **Declined:** *auto-merging above some similarity score* — that is the merge AC-001 withholds, reintroduced with a number in front of it; *recomputing candidates at read time* — §6 forbids it and the source files may be gone; *resolving through a `PATCH` on the asset* — the resolution is a decision with an actor, not a field edit. | REQ-F-001, AC-001, REQ-F-009 | ATEST-001, UTEST-008, ITEST-001 | Developer | **proposed** |
| CHG-049 | 2026-08-16 | `frontend-component-spec.md` — the crew staging panel; `api-specification.md` — two endpoints | TECH v3.32 → v3.33 (**proposed**) | **A crew staging plan that records and dispatches nothing.** The design puts depot counts with steppers beside the ranked table. This is REQ-F-005's placement, at a different granularity: today `PlacementForm` records a crew against a list of assets, and the panel records **how many crews wait at each depot**. Depots are derived from the manifest's `service_areas[]` — the same block CHG-011 added — so no new geography is invented and nothing finer than a service area is stored. **No per-depot recommendation is computed, because none can be defended:** assets carry no service-area column in the prepared format, `outages.csv` is the only mapping between the two and §1 forbids it feeding anything at run time — so a per-depot figure would be invented geography wearing arithmetic's clothes. The panel states the count of high-band assets as context and recommends nothing per depot; if the format later carries `service_area_id` on an asset, this is the entry to reopen. **BR-001 is unchanged and this entry exists partly to say so:** the panel writes a `crew_staging` row — its own table, actor and time on the row, for CHG-048's structural reason (the audit table's `kind` alphabet is frozen) and CHG-015's line (a staging count is an operational note, not a decision about a recommendation) — and nothing else. No crew is moved, no roster is touched, no message leaves the platform, and there is no outbound path to any system that could move one. **Declined:** *letting the recommended figure default into the operator's field* — a recommendation a person did not choose becomes a decision nobody took; *deriving depots from asset coordinates by clustering* — it would invent places the utility does not have; *an *Apply* or *Dispatch* button anywhere on the panel* — there is nothing for it to do, and a button that appears to act is worse than none. | REQ-F-005, BR-001, REQ-F-009 | E2E-001, ITEST-005 | Developer | **proposed** |
| CHG-050 | 2026-08-16 | `database-design.md` §3 — `assets.is_critical_facility`, `damage_reports.customers_out`; `api-specification.md` — the board response | TECH v3.33 → v3.34 (**proposed**) | **The repair queue needs an order, and it must not be a risk order.** The design's queue is ordered with *High* / *Medium* / *Standard* beside each row, and the client's prompt says *ordered by impact*. This platform has a standing decision that **nothing on the dispatch board is ordered by a score** — `priority_rank` has been null since TASK-005 — because the board is during-storm damage that has already happened, and ranking real damage by a predictive model is a different and worse product. **Both hold, because impact is not risk.** Priority is derived from two facts about what has occurred: whether the damaged asset is a **critical facility** (a hospital feeder, a water plant), and how many **customers** the report accounts for. Neither comes from `risk_scores`, neither uses a weight, and the scorer is not consulted. Adds the two columns the derivation needs — `assets.is_critical_facility`, already a column in the client's own file format and never stored, and `customers_out` on the report. **Declined:** *ordering by `risk_scores.rank`* — that is the decision this entry preserves, and it would put a 72-hour forecast in front of a substation that is already down; *a fourth priority level* — three is what the design carries and what a dispatcher can hold; *the words "Critical" or "Standard" for the top band* — the frozen vocabulary is `High` / `Medium` / `Low` everywhere a band is rendered, and a second scale that reads like the first is how they get confused. | REQ-F-007, REQ-F-008, AC-007 | ATEST-007, ITEST-003 | Developer | **proposed** |
| CHG-051 | 2026-08-16 | `data-and-integration-spec.md` §1 — `design_references` in the manifest; supersedes half of CHG-014 | TECH v3.34 → v3.35 (**proposed**) | **CHG-014's constants move from the code to the manifest, and keep the code as their fallback.** CHG-014 found that two of ADR-007's four factors compare against reference values existing in no document, and resolved them as per-asset-type engineering constants carrying their sources — declining per-asset columns because *220 invented numbers with no source is worse than 4 with one*. That reasoning stands. What has changed is that the client's file format now carries a `design_references` block keyed by asset type, giving `design_gust_mph` and `service_life_years` — so the utility can state **its own** design basis instead of accepting ASCE 7 category figures, which is exactly the calibration CHG-014 named as its exit condition. The scorer reads the manifest first and falls back to `ASSET_TYPE_REFERENCES` when the block is absent, so every existing fixture scores identically and nothing in the suite moves. **Declined:** *requiring the block* — every prepared scenario in the repository and in the client's hands would stop loading; *removing the fallback once the block exists* — the fallback is what makes the constants' source visible when a manifest omits them, and CHG-014's argument was about sourcing rather than location; *per-asset design values* — declined by CHG-014 for reasons unchanged. | REQ-F-002, REQ-F-001 | UTEST-010, FTEST-004, EVAL-001 | Developer | **proposed** |
| CHG-052 | 2026-08-16 | `ADR-001` / `fitness-functions.md` FF-001 — a fifth backend module, `summary`, and the `MODULES` tuple that governs it | TECH v3.35 → v3.36 (**proposed**) | **CHG-040's guardrail needs somewhere to live, and putting it anywhere existing would be wrong in a way that matters.** The verifier is not scoring (it computes no risk), not loading (it parses no file), not storage, and it must not be in the API layer, because a rule buried in a request handler is the rule this register has watched decay four times. Adds `backend/app/summary/`: `figures.py` assembles the fixed set the model may see, `draft.py` holds the server-side model call, and `verify.py` is **pure** — no imports from any other module — so the check that decides whether generated text may be shown can be tested against a string and nothing else. **The tuple is the point of this entry.** `ci/fitness.py`'s `MODULES` is `("api", "scoring", "loader", "store")`; a fifth package not in that tuple is a package **FF-001 cannot see**, so a cycle through it would be invisible to the gate that exists to refuse cycles. The tuple is extended in the same commit that creates the directory. The permitted direction is `api → summary → store`, with `verify` a leaf. **Declined:** *`scoring/summary.py`* — FF-002 requires `scoring` to be imported by `api` and nothing else, and it would put text generation inside the module ADR-005 defines as *deterministic arithmetic a person can argue with*; *`api/summaries.py` holding the verifier too* — the orchestration lives there and the rule does not; *a fifth package left out of the tuple* — a fitness function that cannot see a module does not govern it, which is CHG-010's finding in a new place. | REQ-NF-005, REQ-F-011 | FF-001, FF-002 | Developer | **proposed** |
| CHG-053 | 2026-08-16 | `security-specification.md` §7; `database-design.md` §3 — `users.must_change_password`, `users.temp_password_expires_at` | TECH v3.36 → v3.37 (**proposed**) | **CHG-004's admin-set temporary password, with the two properties that make it temporary.** CHG-004 removed the reset-link flow when Round 6 confirmed no email service, and replaced it with an admin-set temporary password. It has been a password like any other: it never expires and the holder is never made to change it, so *temporary* was a description of intent rather than of behaviour. Adds `must_change_password` and `temp_password_expires_at`. A temporary password is **single-use** and expires in `TEMP_PASSWORD_EXPIRY_HOURS`; signing in with one leads to a change-password screen and nothing else — the session exists but every other route is refused, so the flag is enforced by the same guard that enforces the roles rather than by a redirect the browser could skip. **Declined:** *a default expiry in code* — ADR-006's rule is that a lifetime is read from the environment and never defaulted, and this is a lifetime; *letting the holder keep the temporary password* — then it is a password an administrator knows, which is the thing this closes; *emailing it* — there is no email service, which is the whole origin of CHG-004. | SEC-A-004, REQ-NF-002, A-003 | STEST-003 | Developer | **proposed** |
| CHG-054 | 2026-08-16 | `frontend-component-spec.md` — the activity rail's wording rule | TECH v3.37 → v3.38 (**proposed**) | **A feed that must not be able to say the system decided something.** The dispatch board's *Recent activity* rail reads `decision_records` and `security_log`. The supplied design fills it with two entries that this product may never render: *"System auto-flagged Bayside Substation as critical priority"* and *"Forecast data sync completed successfully"*. The first says the system prioritised, which is the sentence `intent.md` was written against — **it recommends; people decide** — and the second describes a live connection to a system this platform has no path to and will not have at any version. The rule: the rail records **human actions** (*"J. Ruiz accepted the ranking for Bayside Substation"*) and **system events** (*"Scenario loaded from 4 files"*), and never a system decision. It is enforced where it can fail, not in a style note: the phrasings are assembled from the record's `kind` and actor, there is no free-text path into the rail, and a test asserts that a feed rendered from an accept, an adjust and a dismiss contains no wording implying the system judged anything. **Declined:** *rendering the design's two entries as written* — they are the clearest possible statement of the thing the product is not; *a style guideline in the component's docstring* — the four other cases in this register where a rule lived only in a comment are the reason; *dropping the rail* — the audit trail exists and a person should be able to see it without a database client. | BR-001, REQ-F-009, REQ-R-001 | ATEST-014, ITEST-002 | Developer | **proposed** |
| CHG-055 | 2026-08-16 | `api-specification.md` — `POST /scenarios/{id}/triage`; the meaning of `decision_records.kind` for subject `asset_ranking` | TECH v3.38 → v3.39 (**proposed**) | **A decision about one asset's rank, recorded — the evidence that operators acted on the rankings.** The design's row menu, asset drawer and Focus Mode all offer *Accept / Adjust / Dismiss* per asset, and the client's prompt is explicit that each writes a decision record. The platform records only whole-ranking decisions (REQ-F-006's accept/change/reject on a recommendation), so triage had nowhere to land. **No schema change, and that is the design:** the audit table's `kind` alphabet is frozen (CHG-036 ruled out the rebuild), and it does not need widening — *accept*, *change* and *reject* are exactly what a person does to one asset's rank, under `subject_type = 'asset_ranking'` with the stored `risk_scores` row as subject. Rendered as the design's words — Accept / Adjust / Dismiss — and stored as the schema's, with the mapping in one place; Adjust and Dismiss require a note for REQ-F-006's reason (the note is why the record is worth keeping). **Unlike a whole-ranking decision there is no one-per-subject rule**: a person may triage the same asset again as the storm moves, and each pass is a new appended row — the history is the point. Nothing is dispatched, removed from the ranking, or re-scored: a Dismiss records disagreement with a rank, it does not hide the asset (an asset hidden by a click is the empty screen that reads as safety). | REQ-F-006, REQ-F-009, BR-001 | ATEST-014, ITEST-002 | Developer | **proposed** |
| CHG-056 | 2026-08-16 | `data-and-integration-spec.md` §1 — the loader accepts the client's canonical dialect; `database-design.md` §3 — `assets.design_gust_mph`, `assets.service_life_years` | TECH v3.39 → v3.40 (**proposed**) | **The client's own dataset could not be loaded, and the refusal was a 500.** The Hurricane Delia pack is written to the client's build prompt — `customers_served` where CHG-011 wrote `customer_count`, `external_id` for `asset_id`, `asset_type` for `type` with seven capitalised type names, condition ratings as the **words** good/fair/poor, a `work_type` column deciding which maintenance rows are failure evidence, and one combined weather shape where every row carries the grid cell, the asset, the forecast gust and a mostly-absent `station_gust_mph`. The first mismatched key raised a raw `KeyError` and the person saw *Something went wrong* — three times, recorded in the log. **Two decisions.** *(a) The loader speaks both dialects*, by named alias rather than by guess: each field reads its CHG-011 name or its client-prompt name; the seven type names map onto the four scoring categories through a written synonym table; good/fair/poor map to the numeric scale through the client's own severity equivalences (0.2/0.55/0.9 → 4.0/2.25/0.5 on the 1–5 scale, exactly); with a `station_gust_mph` column present, the forecast series comes from the dense grid gusts and defect 3 counts the station column's absences; with `work_type` present, defect 6 counts the `routine` rows instead of matching note text. *(b) A design basis the manifest states per dialect type is stored per asset* — `Distribution` is built to 90 mph and `Transmission Line` to 140, both map to category `line`, and a single per-category number would misapply the utility's own basis to half its lines. Two nullable columns on `assets`, resolved at load, read first by the scorer; CHG-014's per-asset refusal was about **invented** numbers, and these are supplied ones. **And the guard that outranks both:** `load_scenario` can no longer surface anything but `LoadFailed` — an unexpected exception in parsing user bytes becomes a named 422 naming the stage and the exception type, never a 500, because a raw error on user input is the exact thing the seven defect rules were written against. **Recorded, not built:** the dialect's `assets.service_area_id` supplies the asset-to-area mapping CHG-049 said did not exist; reopening the per-depot recommendation is that entry's decision to take, not this one's. | REQ-F-001, REQ-F-002, REQ-NF-003, AC-001 | UTEST-006, FTEST-001, FF-006, ITEST-001 | Developer | **proposed** |
| CHG-057 | 2026-08-16 | `frontend-component-spec.md` — the Storm Planning screen's shape | TECH v3.40 → v3.41 (**proposed**) | **The client's second feedback round reshapes the planning screen, and each ask is recorded with what it does not change.** *(a) The screen opens with the answer*: the headline states "N of M assets are at high risk", and beneath it the four highest-ranked assets render as cards — **Highest risk right now** — each carrying its strongest computed reason and a Review action that opens the same drawer the table row does. Nothing is computed for the cards: they render the `RiskItem`s already in the ranking response, which is what keeps the row, the card and the drawer one object (FF-002 untouched). *(b) The forecast-revision control collapses to one line* — current revision, the pending count, and the one apply button — with the per-revision chips kept behind a **Compare an earlier ranking** disclosure. CHG-027's rule stands at a smaller size: a forecast that is coming is still stated (as the count), a revision without an order behind it is still offered only as disabled, and the previous order stays reachable (AC-005) one click deeper than before. ATEST-005 and E2E-001 open the disclosure before touching a chip — a deliberate UI decision, not a test accommodation. *(c) The ranked table pages client-side*: 25, 50 or 100 rows per page (default 100), Previous/Next beneath the table, and the page context stated as "Showing X–Y of Z". `lib/api.ts` follows `next_cursor` until the ranking is exhausted, so pagination is presentation over the whole stored order, never a second read path — and **the unscored group renders on every page**, because an asset nobody could judge must never be hidden by a page boundary (the empty-screen-reads-as-safety rule applied to page 2). | AC-005, REQ-F-003, REQ-F-004 | ATEST-005 (updated), E2E-001 (updated), INTERFACE browser cases | Developer | **proposed** |
| CHG-058 | 2026-08-16 | `frontend-component-spec.md` — the map panel; `constraints-and-non-goals.md` — CON-006: the CHG-041 Google Maps decision is **withdrawn before any key was ever configured**, replaced by Leaflet with OpenStreetMap raster tiles | TECH v3.41 → v3.42 (**proposed**) | **The map becomes a real basemap with no key, no account and no paid service.** The client supplied the implementation shape themselves — Leaflet, OpenStreetMap tiles, circle markers coloured by band — and it removes CON-006's second paid service rather than adding one. **What leaves the browser is the same class of disclosure CHG-041 recorded, restated for the new provider:** tile requests carry the viewport's tile coordinates to the OpenStreetMap tile servers; markers are placed client-side and the asset list never leaves the machine. The required attribution is shown. The SVG scatter fallback is retired — it existed for the missing-key state, and there is no key to be missing; offline, the markers still render over an unloaded base, which is a degraded picture rather than a broken screen. `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY` is retired with it. **One CHG-041 declination is reversed knowingly:** the panel gains a legend and a "{n} mapped" count — the client asked for the reference look, and the panel's dots now sit far enough from the table's band words that colour was becoming the only signal, which the design forbids. Still declined, same reasons as CHG-041: storm track, cone, weather overlays, and any plotted damage report (CON-003 — nothing finer than a neighbourhood exists to plot). Leaflet is imported only inside the browser-side effect: it touches `window` at import time, and the render path stays clean for the Node half of FF-003(c). | REQ-F-003, CON-003, REQ-NF-007, CON-006 | INTERFACE map case | Developer | **proposed** |
| CHG-059 | 2026-08-16 | `ai-boundary-spec.md` §2 — a second, narrower prompt surface; `database-design.md` — `asset_summaries`; `api-specification.md` — `GET/POST /scenarios/{id}/asset-summaries` | TECH v3.42 → v3.43 (**proposed**) | **A per-asset summary, phrased once, stored, and never re-inferred on read.** Each ranked row gains a Summary action: the server drafts a short factual paragraph for that asset and the popup shows it with the verification behind it. **The prompt surface is a fixed figure set built only from the asset's stored `risk_scores` row**: the scoring category, band, rank and ranked total, the computed reasons verbatim (factor, strength, contribution, detail) — or the unscored reason — and the forecast timestamp. **No asset name, identifier, coordinate or note enters the prompt**: ADR-009's boundary holds here *without* CHG-040's top-asset-name widening, because the popup's title is the client's to render and the model has no need of it. Verification is the same pure machinery with **one recorded difference**: `mph` is not forbidden vocabulary on this path — a gust reason legitimately states the gust in mph, and the figure check already pins every number in the draft to a supplied value, which is the actual guard. One regeneration on failure; a second failure stores the template assembled from the computed reasons, labelled `Assembled from computed factors`. **Stored once per (scenario, asset, forecast_revision)** — `unique` in the schema, so a repeat request returns the stored row (the cache is the constraint, not a code path), and a re-rank produces a fresh summary for the new revision rather than a stale one relabelled. Model attempts are stored per row and counted by the **same guards as the situation summary** — one shared monthly ceiling, one per-scenario call bound — so the per-asset path cannot spend what the dispatch path was budgeted. The GET lists stored rows only and is read-stable (FF-003); generation is the POST's alone. | REQ-F-003, ADR-009, REQ-NF-002 | test_CHG-059 unit + integration, INTERFACE summary case | Developer | **proposed** |
| CHG-060 | 2026-08-16 | `frontend-component-spec.md` — the Storm Planning screen loses three sections at the client's instruction | TECH v3.43 → v3.44 (**proposed**) | **The client's second pass over the reshaped planning screen: three sections removed, the default page size lowered, and one stacking defect fixed.** *(a) "Since you last looked" is removed.* The movement strip did not read as movement to its one reader. The fact is not lost: rank-movement arrows stay beside each rank in the table, the headline still states how many assets moved up, and the stored diff (CHG-044) keeps being written — the strip was one rendering of it, and the other two stand. *(b) The whole-ranking decision form ("Your decision on this ranking") and the crew placement form are removed from the screen.* Per-asset triage is the decision surface now — Accept/Adjust/Dismiss in the drawer and Focus Mode, each writing a decision record (CHG-055) — and the drawer's confirmation now names the forecast revision the decision was recorded against, which is the sentence the placement form used to carry. **What this withdraws and what it does not:** the `POST /recommendations/{id}/decision` and `POST /scenarios/{id}/placements` endpoints stand, their API-level proofs stand (ITEST-002, test_E2E-001 integration, the migration-012 trigger), and BR-001 is untouched — but **REQ-F-005 has no screen path after this change**. A placement can be recorded only through the API. Whether a placement gets a new home on some screen is the client's open question, recorded here rather than answered by guessing; the crew staging plan (CHG-049) remains the screen-level record of where crews wait. E2E-001's browser half is rewritten around the drawer: a decision recorded from the screen against the revision being read, a failed write that keeps the typed note (FTEST-005's screen half), and a failed ranking read that offers no decision against nothing. *(c) The ranked table defaults to 25 rows per page*, expandable to 50 or 100 — CHG-057's mechanism, the client's default. *(d) A defect, fixed rather than decided:* Leaflet's internal panes carry z-indexes in the hundreds and the map was painting **over** the asset drawer; the map host now establishes its own stacking context (`relative z-0`), so the drawer sits above the map everywhere, held by a pointer-interception check in the browser suite rather than by a class name. | REQ-F-002, REQ-F-005, AC-005, BR-001 | ATEST-005 (updated), E2E-001 (rewritten), INTERFACE (updated) | Developer | **proposed** |
| CHG-061 | 2026-08-16 | `security-specification.md` §7 — reversed for exactly one shape; `api-specification.md` — `POST /api/v1/auth/signup` | TECH v3.44 → v3.45 (**proposed**) | **Self-service sign-up, at the client's instruction — operator only, and the role is not a parameter.** §7's "roles are set in the database, not through any endpoint; no self-service promotion path, at any version" held from Round 6 until today, and the half about *promotion* still holds entire: the new endpoint creates **operator** accounts and nothing else, the request model forbids unknown fields (a body that tries to name a role is refused, not stripped), and an admin account still exists only through the CLI on the host. The platform's one password policy applies — 12 characters, the same bound the CLI and the password change enforce — and blank is judged by the shared alphabet, never `str.strip()` (CHG-023's sentence, applied on the way in). A successful sign-up signs the caller in: same session, same cookie flags as the sign-in path. **Two costs, taken knowingly:** *(a)* a duplicate-email refusal necessarily confirms an address is in use — real anti-enumeration for sign-up needs an email-verification system, and none exists (CON-002); STEST-003's protection of the *sign-in* path is untouched. *(b)* Anyone who can reach the host can now make themselves an operator — acceptable for an internal demo deployment, and the reason the operator role can read and decide but can neither load a storm nor resolve identity. **Also recorded here:** the developer seeded `test@example.com` (password `test`, admin) directly into the *development database* at the client's spoken instruction — a seed row in one dev database, not a policy; the sign-up endpoint would refuse that password. | SEC-A-002, REQ-NF-004 | test_CHG-061 (6 cases), STEST-001 census, INTERFACE sign-up case | Developer | **proposed** |
| CHG-062 | 2026-08-16 | `frontend-component-spec.md` — the Storm Planning screen loses two more sections at the client's repeated instruction | TECH v3.45 → v3.46 (**proposed**) | **The forecast-revision control and the "All assets" joined view are removed from the screen.** The client asked twice; CHG-057's compaction of the revision control was not the deletion they meant, and this entry is the deletion. **What is withdrawn:** *(a)* applying the next forecast change has **no screen path** — REQ-F-004's re-rank and AC-005's read-back-for-comparison are now API facts, held by the pytest halves (ITEST-004, test_ATEST-005 integration) while `ATEST-005.spec.ts`, the browser proof, is **deleted with its subject**; the screen always shows the storm's current revision. *(b)* The joined per-asset view with source-and-age columns leaves the planning screen; ATEST-001's claim stays provable at the API, and on screen the same provenance table lives in each asset's drawer — one click away, not gone. The one-source-record-two-codes dedup claim moved from the table to the ranking caption, where the codes already render. **What this costs, stated rather than glossed:** a storm loaded at revision 0 stays at revision 0 until something calls the endpoint — the demo walk toward landfall needs either the API or a future control on some screen, and which is the client's open question. TASK-009's storm-switching proofs re-anchor on the ranking list and the staleness banner; E2E-001 loses its forecast-walk case. | REQ-F-004, AC-005, ATEST-001, REQ-F-010 | ATEST-005.spec.ts deleted (API halves stand), E2E-001/E2E-002/TASK-009 re-anchored | Developer | **proposed** |
| CHG-006 | 2026-08-15 | 20 files across `01-docs/`, `02-tasks/`, `03-tests/`, `07-ops/`; new ADR-006 and ADR-007 | TECH v1.5 → v2.0 | **Fourteen open questions answered after the intake closed.** Q-017 (scenario = manifest + four CSVs, under 5 MB) unblocked TASK-002 and eight downstream tasks. Q-027 (Next.js/TypeScript + SQLite) filled every command in `07-ops/`. Q-007 enumerated the forbidden data. Q-021 and Q-025 became ADR-006 and ADR-007. Q-012 gave measurable limits; Q-013 gave REQ-NF-006 a standard and ATEST-011; Q-015 set retention; Q-011, Q-019, Q-022, Q-024 closed. Q-018 and Q-028 deliberately left open with reasons. | REQ-NF-001, REQ-NF-004, REQ-NF-006, REQ-NF-007, SEC-A-002, SEC-A-004, SEC-A-006, CON-003 | ATEST-011, PTEST-001, PTEST-002 | Developer | accepted |

---

## Stage acceptance and skips

Two things are recorded here as **dated rows**, and neither is ever a file of its own: the
acceptance of each round's gate, and any blueprint deliberately skipped with its reason.

A separate acceptance file would be a second place to look for the same fact, and the two
would disagree within a week. A row in the log that already exists cannot.

**The date is the first column.** That is what makes a row findable — an acceptance buried
in a nine-column change entry is not a record anyone can check, and the change-entries table
above starts with an identifier rather than a date, so it cannot serve.

| Date | Stage or type | Artifact | Note or reason |
|---|---|---|---|
| 2026-08-15 | Round 1 — the idea | — | Accepted by the developer. 7 decisions, 2 inferences, 6 TODOs (Q-001 to Q-006). |
| 2026-08-15 | Round 2 — scope boundaries | — | Accepted by the developer. 6 decisions, 1 inference, 4 new TODOs (Q-007 to Q-010); Q-001 to Q-004 closed. |
| 2026-08-15 | Round 3 — users, roles, and data | — | Accepted by the developer. 8 decisions, 4 inferences, 6 new TODOs (Q-011 to Q-016); Q-010 closed. |
| 2026-08-15 | Round 4 — product shape | — | Accepted by the developer. 9 decisions, 2 inferences, 3 new TODOs (Q-018 to Q-020); Q-005 closed. |
| 2026-08-15 | Round 5 — architecture and stack | — | Accepted by the developer. 7 decisions (ADR-001 to ADR-004, DD-001 to DD-007), 1 inference, 2 new TODOs (Q-021, Q-022); Q-016 and Q-020 closed. CHG-002 and CHG-003 raised. |
| 2026-08-15 | Round 6 — security, reliability, integrations | — | Accepted by the developer. 8 decisions (SEC-A-001 to SEC-A-006, SEC-Z-001 to SEC-Z-006), 3 inferences, 2 new TODOs (Q-023, Q-024). CHG-004 raised. `ai-boundary-spec.md` filled rather than skipped. |
| 2026-08-15 | Round 7 — tasks and tests | — | Accepted by the developer. 11 decisions (TASK-001 to TASK-010, A-001 to A-015, 47 test ids), 2 inferences, 1 new TODO (Q-025); Q-009 and Q-023 closed. CHG-005 raised, adding ADR-005 out of round. `ai-evals.md` filled rather than skipped. |
| 2026-08-15 | Round 8 — operations | — | Accepted by the developer. 9 decisions, 1 inference, 1 new TODO (Q-026), deferred at the developer's direction: no real people exist, one person holds every role for the prototype, rollback triggers self-approved, naming left as a production-planning TODO rather than invented. Final round; 39 files written. |

**A skip with no reason is a silent skip wearing a label.** The reason is what lets a later
reader tell a decision from an omission.

---

## When implementation reveals something the spec missed (Ch. 15 §15.8)

| When this happens | Update this document |
|---|---|
| A rule becomes clearer during implementation | Requirements document |
| A design decision changes | Technical specification or ADR |
| A new test case is discovered | Test plan |
| A task produces extra work | Task plan and traceability matrix |
| A behavior is removed or postponed | Scope and out-of-scope notes |

## When production teaches you something (Ch. 3 §3.9)

| Change type | Artifact to update |
|---|---|
| A new user behavior is added. | Requirements and product specification. |
| A data field or relationship changes. | Technical specification and data model. |
| A new security rule is added. | Requirements, technical specification, test plan. |
| A bug reveals missing expected behavior. | Requirement, test plan, task history. |
| Deployment process changes. | Deployment checklist and maintenance notes. |

---

## Spec update fields (Ch. 24 §24.7)

| Field | What to write |
|---|---|
| Change date | When the spec was updated. |
| Reason | Bug fix, user feedback, performance issue, security finding, product decision. |
| Affected requirement | The requirement ID or section that changed. |
| Affected tests | Which tests need to be added or changed. |
| Affected code area | The module, endpoint, page, job, or service connected to the change. |
| Review status | Draft, reviewed, approved, implemented, or released. |

---

> **Spec drift warning (Ch. 15 §15.8):** spec drift happens when the code changes but the
> specification stays behind. The longer you allow drift, the harder it becomes to trust
> the source of truth for your project.

---

> Blueprint: blueprints/01-docs/09-change-control/spec-change-log.md
