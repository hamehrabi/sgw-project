# Open Questions

> Source: Appendix A, Ch. 7 §7.11, Appendix C.
> Open questions must be **captured**, not hidden. An unresolved question that reaches an
> AI agent becomes a silent assumption — and silent assumptions become defects.

> **Technical spec guardrail (Appendix C):** open questions must not be treated as
> assumptions.

| ID | Question | Why it matters | Decision owner | Must be answered before | Status | Answer / decision |
|---|---|---|---|---|---|---|
| Q-001 | Which capabilities must exist in version one? | Nothing downstream can be written without it. The requirements, the data model, and the task plan are all derived from this list. | Product owner (not yet named) | Design | Answered | Four, all in version one: the joined asset view on prepared data; the ranked risk list with plain-words reasons; the planning view for the operations manager; the dispatch board for the dispatcher. (Round 2) |
| Q-002 | Which capabilities are explicitly out of scope for version one? | A capability nobody ruled out will be assumed in by whoever reads the specification next. An explicit no is a decision; silence is an accident. | Product owner (not yet named) | Design | Answered | Four, all deferred rather than rejected: live connections to the four source systems; water early-warning on sensor readings; the automatic summary writer; the offline crew app, field photo capture and route planning. Recorded with reasons in [`constraints-and-non-goals.md`](constraints-and-non-goals.md). (Round 2) |
| Q-003 | What hard constraints already exist — no paid third-party services, a single small server, data that cannot be stored, a mandated technology? | A constraint changes the architecture rather than decorating it, and an unstated one silently rules out designs that have already been chosen. | Tech lead (not yet named) | Design | Answered | Two selected: no paid third-party services (CON-006), and certain data must not be stored (CON-003 — subject named in Q-007). Not selected, and recorded as such: no mandated technology (CON-001), no single-server ceiling (CON-004). (Round 2) |
| Q-004 | Of the version-one capabilities, which single one does this product actually compete on? | It decides where the depth goes. Getting it wrong is how a first version spends its whole build horizon on the part nobody chose it for. | Product owner (not yet named) | Design | Answered | The ranked risk list with plain-words reasons. It is the only Core row in [`subdomain-map.md`](subdomain-map.md); everything else is Supporting or Generic. (Round 2) |
| Q-005 | Do the source PRD's success measures apply as version-one targets, given a build horizon of about one week? | Two of them — seven in ten real failures flagged in advance, and a storm plan built in under an hour — presuppose a risk model and live data connections a one-week version may not contain. Carried forward unconfirmed, they read as commitments. | Product owner (not yet named) | Design | Answered | Partly. Version one's success is **a named action completing faster than today**: the manager reaching a crew placement, and the dispatcher reaching one damage picture. Those two PRD figures carry over as starting targets. The other two do not: *seven in ten failures flagged* needs a model validated against SGW's own failure history, and *operators change a plan in one storm in three* is a Phase 1 exit test, not a first-month metric. Both absences are stated in `product-spec.md` §4. The baseline remains unmeasured — Q-018. (Round 4) |
| Q-006 | Is the problem statement in [`project-brief.md`](project-brief.md) correct as written? | It was drawn from `proj-knowledge/PRD-SGW-brochure.pdf` rather than stated during the interview. Every requirement in this workspace descends from it, so an error here propagates everywhere. | Product owner (not yet named) | Design | Open | |
| Q-007 | Which data specifically must the system not store? | CON-003 records the restriction without its subject, and the three candidates behave differently: personal data changes the data model, outage data at household level changes what the screens may show, and health or payment data would change the compliance posture entirely. | The developer | Design | **Answered** | **Forbidden in the database, in logs, and on screen:** customer names, addresses, account numbers, meter IDs, phone numbers, any premise-level record; individual household outage status; crew personal data beyond a display name and role; credentials, session values and reset links. **Permitted:** aggregate outage counts by feeder or neighbourhood, and a `critical_facility` boolean on an asset. This is the source PRD §8 privacy line — *neighbourhood totals rather than single homes* — stated as a schema rule. (CHG-006) |
| Q-008 | Does the one-week horizon cover all four version-one capabilities, or a scenario-driven probe of them on one prepared storm? | Four capabilities against about one week is a tension rather than a contradiction, so it has been recorded rather than resolved. The answer sets what "done" means for every task in Round 7. | Product owner (not yet named) | Design | Open | |
| Q-009 | Who or what builds this, and with what skills? | CON-008 stays empty until this is answered, and the answer decides whether `06-agent/` is the centre of this workspace or a formality. | Tech lead (not yet named) | Implementation | Answered | An AI coding agent, one task at a time. CON-008 now says so. The consequence is that every task file's *do not change* list and *stop condition* are enforcement rather than advice, and Round 8's `06-agent/` becomes the most-read folder in the workspace. (Round 7) |
| Q-010 | What is the permission model, and is sign-in inside version one at all? | [`subdomain-map.md`](subdomain-map.md) classifies authentication as Generic — work to keep cheap — but cannot say whether to build it thin or leave it out, and roles cannot be retrofitted without touching every read. | Tech lead (not yet named) | Design | Answered | Two roles, admin and user, and sign-in is inside version one (REQ-NF-002). The admin loads scenarios and reads the decision record; the user does everything else. Deciding on a recommendation is deliberately not privileged — it is the product. (Round 3) |
| Q-011 | Are the weather forecast per grid area and the maintenance and inspection history stored as entities, or read from the prepared files at scoring time and preserved only inside a risk score's reasons? | It decides whether the data model gains two more tables, and whether an earlier ranking can be re-derived from its inputs or only re-read as it was recorded. | The developer | Design | **Answered** | **Yes — both become scenario-scoped, read-only reference tables.** They are inputs to scoring, not things the system reasons about independently. This makes an earlier ranking re-derivable from its inputs rather than only re-readable as recorded. (CHG-006) |
| Q-012 | What are the real performance and usability limits — re-rank latency, dispatch-board update latency, and how many actions reach a critical control? | REQ-NF-001 and REQ-NF-004 carry figures the source PRD itself labels as starting targets rather than measurements. A limit nobody measured can be neither passed nor failed, so it is not yet a requirement. | The developer | Design | **Answered** | Re-rank 220 assets **under 5 s**; page load **under 2 s**; reason panel open **under 300 ms**. **Measured on the fixture, not promised** — which is the difference between these and the figures they replace. Unblocks PTEST-001 and PTEST-002. (CHG-006) |
| Q-013 | Is there an accessibility standard this must meet, and is it a driver or a constraint? | REQ-NF-006 is empty. A control room under storm conditions is exactly the place where contrast, keyboard reach and text size stop being a preference. | The developer | Design | **Answered** | **WCAG 2.1 AA as the target.** A constraint, not a driver — the three drivers stand. `frontend-component-spec.md` already carries keyboard operability on `RiskList` and the rule that colour is never the only signal, which covers much of it. Closes the traceability gap where REQ-NF-006 had no design link and no test. (CHG-006) |
| Q-014 | Does version one store files that users upload or generate — specifically, does an admin upload the prepared scenario through the application, or is it placed on disk at deploy time? | Yes brings size limits, content-based type verification, malware scanning and a retention rule, none of which no implies. It is the clearest single yes/no in this workspace that changes what gets built rather than how it is described. | Tech lead (not yet named) | Design | Answered | Yes — an admin uploads by drag-and-drop through the application, and several storms may be loaded at once rather than one replacing another. Recorded as CHG-001; it raised Q-017 and made an upload timeout and size limit required. (Asked and answered between Rounds 3 and 4) |
| Q-017 | What does one prepared scenario consist of — which file formats, how many files, and how large? | Confirming the upload path made a size limit and a format allow-list mandatory, and neither can be set by guessing: an unbounded upload is a denial of service and a bill, and a deny-list is only a list of the attacks somebody already thought of. | The developer | Design | **Answered** | One folder or zip: `manifest.json` plus four CSVs — `assets.csv`, `maintenance.csv`, `weather.csv`, `outages.csv`. Nothing else. Demo scale: 220 assets, ~2,000 maintenance rows, ~5,000 forecast rows, ~300 outage rows, **under 5 MB total**. Every column traces to the source PRD §7 dataset table; nothing invented. The shipped fixture carries all seven §7 defects on purpose. Full column list in [`data-and-integration-spec.md`](../06-api-and-data-design/data-and-integration-spec.md) §1. Unblocks TASK-002 and PTEST-001/002. (CHG-006) |
| Q-015 | What retention and deletion rules apply to each entity, and in particular how long is the decision record kept? | The decision record exists to be produced to a regulator. "Kept forever" is a decision; "until someone deletes it" is not, and the difference decides the backup design in Round 8. | The developer | Release | **Answered** | **Decision record: indefinite** — it is regulatory evidence. **Scenario data: 90 days after storm close. Application logs: 30 days.** The asymmetry is the point: everything re-derivable expires, and the one thing that cannot be reconstructed does not. (CHG-006) |
| Q-016 | Which authentication model — email and password with server-side sessions, a third-party identity provider, OAuth, or passwordless? | CON-006 rules out buying an identity provider in version one, which narrows the choice without deciding it. The API contract cannot be completed until it is decided. | Tech lead (not yet named) | Design | Answered | Email and password with server-side sessions, recorded as [ADR-003](../05-architecture/architecture-decisions/ADR-003-email-password-sessions.md). Magic link was rejected on a stronger ground than cost: an authentication path that depends on email delivery degrades exactly when a storm is degrading everything else. It does not settle session lifetime (Q-021) or the second factor (Q-022). (Round 5) |

| Q-018 | What is today's measured baseline for the two named actions — how long does building or adjusting a storm plan actually take, and how long does one full damage picture take? | Version one's success metric is "faster than today", and nobody has measured today. The figures in `product-spec.md` §4 come from the source PRD's own reasoned estimate, which that document labels a guess to be replaced by observation in the first two weeks. A target measured against an estimated baseline can be hit while nothing improved. | The developer | Release | **Open — deliberately, and permanently for this build** | **Cannot be measured: the client is fictional.** It remains the source PRD's assumption A6, and Phase 0 — two weeks in a real control room — exists to replace it. **This one stays visible rather than being quietly closed.** The ROI argument leans on a $20M storm-season baseline that the briefing states is an illustration; leaving it flagged as unmeasured is what makes the rest of that argument credible. (CHG-006) |
| Q-019 | What monthly running cost, once exceeded, means somebody must be told? | Cost behaves like latency: unmeasured, it only ever surfaces as a surprise. A ceiling with an alert is the cheapest governance in the workspace, and CON-006 makes it a small number worth naming rather than a large one worth avoiding. | The developer | Release | **Reopened** | Answered as *zero recurring spend* in CHG-006, then **reversed by ADR-009** — there is now a metered OpenAI invoice. `LLM_MONTHLY_COST_CEILING_USD` and `LLM_MAX_CALLS_PER_RANKING` are required and unset. A ranking phrases up to 220 reasons; unbounded, one storm is an unbounded bill. (CHG-007) |
| Q-020 | Which architecture style — modular monolith, simple monolith, service-based, or serverless? | It is shape rather than detail, and shape is the one thing this kit forbids inferring. Everything in `technical-spec.md` §2 and the whole architecture folder are written against it. | Tech lead (not yet named) | Design | Answered | Modular monolith — one deployable process, five named modules, a one-directional import rule. Recorded as [ADR-001](../05-architecture/architecture-decisions/ADR-001-modular-monolith.md). The deciding factor was that a simple monolith leaves FF-001 and FF-002 with no boundary to inspect. (Round 5) |
| Q-021 | How long should a session last before it expires? | ADR-003 requires that a session expires and that expiry is checked server-side on every request, but not how long. A control room signed in for a fortnight and a control room signed out mid-storm are different failures, and the number is what chooses between them. | The developer | Implementation | **Answered** | **240 minutes idle, 12 hours absolute.** Utility control rooms run 12-hour shifts, and an operator logged out mid-storm is a safety problem rather than a security win; the platform is read-only toward the grid, so a stale session's blast radius is a view, not an action. **Admin actions re-authenticate regardless of session age.** Recorded as [ADR-006](../05-architecture/architecture-decisions/ADR-006-session-lifetime.md). (CHG-006) |
| Q-022 | Does version one require the second authentication factor the source PRD asks for? | The source PRD (§8) requires it for the full platform. Adding it to a one-week probe is real work; leaving it out of anything that touches critical-infrastructure data is a real exposure. Neither answer is safe to assume. | The developer | Release | **Answered** | **None in version one** — no external service is permitted (CON-006, Round 6). Recorded as **P1: TOTP, never SMS.** The exposure is accepted knowingly rather than overlooked, and it is bounded by ADR-006's re-authentication on admin actions. (CHG-006) |

| Q-023 | Is version one's risk score produced by a trained model, or by a deterministic rule over the prepared data? | It decides whether `ai-evals.md` needs a held-out evaluation set and a model version recorded on every ranking, or a set of worked examples and a rule review. Both are honest; only one is what gets built, and the source PRD assumes the first while a one-week horizon points at the second. | Tech lead (not yet named) | Design | Answered | A **deterministic weighted rule** for version one, behind the model-shaped boundary in `ai-boundary-spec.md` §2, so a trained model can replace it without changing anything outside the scoring module. Deep learning is rejected for this scorer permanently on explainability; the decision-tree ensemble family is the successor when SGW supplies failure history. Recorded as [ADR-005](../05-architecture/architecture-decisions/ADR-005-deterministic-scoring-behind-a-model-boundary.md) and CHG-005. It raises Q-025. (Round 7) |
| Q-025 | Which factors enter the version-one scoring rule, with what relative weights, and who signs off that the resulting ranking is sane? | ADR-005 fixes the *kind* of scorer and not its content. A weighted rule is trivial to adjust until the ranking looks agreeable, and a rule tuned to look right is indistinguishable on screen from one that is right — with reasons that are wrong in the same confident way. This is the substance of the core subdomain, not a parameter. | The developer; **calibration owed to SGW's engineers** | Design | **Answered** | Four factors: forecast gust vs design threshold **0.40**, flood zone **0.25**, age vs service life **0.20**, condition decayed by inspection staleness **0.15**. Score = 100 × weighted sum; bands High ≥ 60, Medium 30–59, Low < 30. Reason strength falls out of the same arithmetic — ≥ 25% of total "Strong", 10–25% "Moderate", < 10% "Slight" — so reasons are computed, never authored. **Criticality is kept separate from risk.** Recorded as [ADR-007](../05-architecture/architecture-decisions/ADR-007-scoring-factors-and-weights.md). **The weights are an assumption to be calibrated with SGW's engineers, and the operator sessions exist to challenge them.** (CHG-006) |
| Q-024 | Is admin-performed password reset the right answer, given no email service? | Round 5 chose email and password; Round 6 removed every external service, so no reset link can be sent. An admin setting a temporary password honours both answers, but it was derived rather than chosen, and it puts one person in a position to take over another's account. | The developer | Design | **Answered** | **Yes, confirmed, with two conditions the derived version did not have:** the temporary password is **single-use** and **expires in 24 hours**, and the user must change it at first sign-in. Both narrow the window in which one person can take over another's account. SEC-A-004 and SEC-Z-006 gain them. (CHG-006) |

| Q-026 | Who are the named people behind the decision-owner roles in this register? | Every row here names a role — *Product owner (not yet named)*, *Tech lead (not yet named)*. That was honest during an interview with one participant, and it stops being sufficient the moment a question needs answering: a question owned by a role is a question nobody answers. Twelve questions are open and none has a person attached. | The developer | Design | **Deferred** | No real people exist. For the prototype, I am the sole owner of every decision-owner role. In the PRD, the named owner is the operations director — a role, not a person, and explicitly flagged as to be confirmed with the client. The three rollback triggers are self-approved for this build. This stays a TODO for production planning rather than being resolved with invented names. |

| Q-027 | Which technology stack — language, framework, and test runner? | CON-001 records that no technology is mandated, and Round 8 chose a deployment *shape* (a container on a cloud VM) rather than a stack. Every build, lint, test and start command in `07-ops/` is a marker until this is answered, and so is the naming of every executable test file. | The developer | Implementation | **Answered** | **Next.js (TypeScript) with SQLite via `better-sqlite3`, one process.** Lint `eslint` + `prettier`; unit and integration `vitest`; acceptance `playwright`; migrations `drizzle-kit`; run `next dev` / `next start`. Chosen because one language gives the agent one toolchain, and a single process matches ADR-001 and ADR-002 exactly. Python/FastAPI was the alternative and was declined: at 5 MB of CSV the data-tooling advantage does not apply. (CHG-006) |
| Q-028 | Has a restore and a rollback been performed and timed? | Both plans are written and neither has been rehearsed. Until one has, the four-hour recovery objective is an aspiration rather than a commitment, and `backup-and-recovery.md` §5 is empty — which that file names as the single most important gap in it. **A backup nobody has restored is not a backup; it is a hope.** | The developer | Release | **Deferred** | Not yet — one rehearsal is required **before the demo**, documented in `backup-and-recovery.md` §5 with its date and elapsed time. Recorded as a commitment with a deadline rather than closed: until the row exists, the four-hour objective remains a hypothesis. (CHG-006) |

| Q-029 | What are the LLM cost guards — max calls per ranking, monthly ceiling, and timeout? | ADR-009 makes all three mandatory and sets none. `runtime-and-scale.md` §1 had already written the trigger: *any endpoint calls a metered service — on that day an unlimited endpoint is an unlimited invoice.* The fallback to computed text makes a hard cap safe to enforce, so there is no reason to leave it open except that nobody has picked numbers. | The developer | Implementation | Open | |
| Q-030 | Which OpenAI model, pinned by version? | ADR-009 forbids `latest`: a silent model change is a silent behaviour change, and `ai-evals.md` re-runs on any change to it. Cost, latency and phrasing quality all move with this choice. | The developer | Implementation | Open | |

**Status values:** Open · Answered · Deferred · Rejected

Decision owners are recorded as roles because no individual has been named for this workspace
yet. Naming them is part of answering Q-001 through Q-006, not a separate question.

---

## The ambiguity test (Ch. 2 §2.6)

Before moving forward, ask: *Could two competent developers build two different things
from this instruction?* If yes, it belongs in this table.

| Ambiguous statement | Why it is dangerous | Clarified version |
|---|---|---|
| "Users can create tasks." | Does not define fields, ownership, deadlines, or permissions. | "A signed-in user can create a task with title, description, due date, priority, and assignee." |
| "Admins can manage users." | Does not say what *manage* means. | "An admin can view users, deactivate accounts, reset roles, and see account status." |
| "The app should be secure." | Too broad to test. | "The app requires login, role-based access, input validation, and safe error messages." |

---

> Blueprint: blueprints/01-docs/01-intent/open-questions.md
